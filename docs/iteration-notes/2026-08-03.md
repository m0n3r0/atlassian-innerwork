# Iteration: 2026-05-29 — 2026-08-03

## Summary

This is the first post-launch iteration note. Phase 10 launched when
PR #19 merged on 2026-05-29, completing the phase 0-10 roadmap: every
phase shipped, and the beta program, migration tooling, launch plan,
and post-launch iteration cadence documented in `docs/post-launch-iteration.md`
are now in effect. The post-launch backlog opened with the
markdown-importer as its first item. The window since launch has been
quiet: no beta issues were filed, and the only landed change beyond the
Phase-10 merge train is a small guardrail fix to the anti-hallucination
scan. The items under "Directional next" in `docs/roadmap.md` are
deferred; the backlog-driven iteration loop has just begun picking them
up.

## Triaged

None. No issues were filed against the beta program in this window
(2026-05-29 to 2026-08-03), so there was nothing to triage.

## Landed

- #13 — docs(phase-f): sync `collaboration.md` with `notify.py` and
  `portability.py` implementation (merged 2026-05-28).
- #14 — Phase 6: search, analytics, AI context boundaries (merged 2026-05-29).
- #15 — Phase 7: trust hardening — audit log, field ACL, threat model
  (merged 2026-05-29).
- #16 — Phase 8: deployment, operations, SLOs, observability, release
  engineering (merged 2026-05-29).
- #17 — Phase 9: governance, contributor experience, packaging
  metadata, docs scaffolding (merged 2026-05-29).
- #18 — docs(phase-10): PM scoping for beta, migration, launch, and
  post-launch iteration (merged 2026-05-29).
- #19 — Phase 10: beta, migration, launch, and post-launch iteration
  (merged 2026-05-29) — the launch.
- `2ea7180` — fix(guardrail): skip `.worktrees/` in the anti-hallucination
  scan (2026-08-03).

PRs #13-#19 merged across 2026-05-28/29, covering the phase-F
collaboration docs sync and phases 6-10. Nothing else landed in this
window.

## Deferred

The items under "Directional next" in `docs/roadmap.md` remain on the
backlog, uncommitted and unprioritised:

- Migration: markdown-importer (first backlog item — in flight per the
  direction notes below), csv-importer. Hosted-Jira and hosted-Confluence
  importers remain explicitly not committed and out of active design.
- Portability format: audit-export-flag (opt-in `--include-audit`),
  streaming-export for very large stores.
- Quality and operability: backup-restore-upgrade procedures in the
  operations runbook, time-windowed metrics aggregations,
  observability-shape documentation (Prometheus / log-scraping; no
  exporter ships).
- CLI ergonomics: innerwork-doctor, friendlier `--help` examples,
  shell completion.
- Documentation: architecture-doc (`docs/architecture.md`),
  operator-cookbook.

These were considered but punted: the window produced no beta feedback
that would reorder them, and the iteration loop has only just begun
picking items off the backlog.

## Direction notes

- Backlog graduation is now driven by the atlassian-innerwork
  orchestrator, which promotes roadmap items into flight instead of a
  manual per-tick selection. The first item in flight is the
  markdown-importer.
- No changes to roadmap priorities, governance, or scope boundaries in
  this window; `docs/roadmap.md` is unchanged.
- This note does not propose graduating the beta program. The
  graduation review described in `docs/post-launch-iteration.md` §6
  remains open for a future tick.

## Late merges (addendum)

Appended after this note was finalised, per the append-only rule in
`docs/post-launch-iteration.md` §3. Three PRs merged after the Landed
list above was written but before the next iteration note; they are
recorded here so the record is complete.

### Landed (additional)

- #21 — docs(markdown-importer): PM scoping for markdown-tree importer
  (merged 2026-08-03T12:39:59Z). Scoping for the markdown-tree importer;
  merged before this note's own PR (#20, 12:46:31Z) and missed from the
  Landed list above — reconciled here.
- #22 — feat(markdown-importer): markdown-tree importer for spaces/pages
  (merged 2026-08-03T13:45:48Z). The first post-launch backlog feature
  shipped: `innerwork import-markdown` reads a local directory tree of
  `.md` files into `spaces` / `pages` (see `docs/migration-guide.md` §4).
- #23 — docs(csv-importer): PM scoping for CSV/TSV importer (merged
  2026-08-03T13:55:08Z). Scoping for the next migration item;
  implementation is now in flight.

### Deferred (correction)

The migration bullet in the Deferred section above is superseded:

- markdown-importer is no longer deferred or in flight — it SHIPPED in
  PR #22 (2026-08-03T13:45:48Z) and is removed from the deferred /
  migration list. `docs/roadmap.md` already reflects it as shipped.
- csv-importer is now in flight: scoping merged in PR #23
  (2026-08-03T13:55:08Z), implementation underway. It remains under
  "Directional next → Migration" in `docs/roadmap.md` as an
  investigation item; this addendum does not re-prioritise it.
- Hosted-Jira and hosted-Confluence importers remain explicitly not
  committed and out of active design.

### Backlog loop

The first post-launch backlog feature shipped the same day the launch
note was published: the orchestrator graduated markdown-importer from
roadmap item to scoping (#21) to implementation (#22) within the same
window, and csv-importer entered flight immediately after via its
scoping PR (#23). The backlog-driven iteration loop is now picking items
off the roadmap rather than only planning them. No beta-user claims, no
participant counts, and no roadmap-priority changes are introduced by
this addendum.
