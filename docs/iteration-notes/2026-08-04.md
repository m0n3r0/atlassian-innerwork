# Iteration: 2026-08-03 — 2026-08-04

## Summary

The read-only audit sweep of phases 0-10 landed this window: the
code-correctness (#25), documentation-accuracy (#27), and security/trust
(#28) audit reports merged at 2026-08-03T16:01Z, adding three reports
under `docs/audit/`. Together they record 10 code findings (0 HIGH /
3 MED / 7 LOW) and surface the consolidated fix backlog (C1-C38) whose
P0 items are the portability permission drop and mixed-case mention
handles. The QA audit (#29) and consolidated report (#30) came back from
review with requested changes and are being corrected. csv-importer
(#26) remains open: its QA gate passed (373 tests on branch vs 332 on
main), with the SEC gate and independent review still pending. This note
covers only the window since the 2026-08-03 note and its addendum (PRs
#20-#24), which remain the record for the earlier merges.

## Triaged

No beta issues were filed in this window. The audit sweep served as the
triage pass over the codebase: four read-only audits of phases 0-10 (PRs
#1-#23 + `main @ 328561d`) reconciled with no cross-report
contradictions and were consolidated into a single deduplicated findings
list (C1-C38) with a unified severity scale and a prioritized fix
backlog (P0-P3). None of the audits changed code; the suite is
untouched.

## Landed

- #25 — audit-code: full codebase audit findings report (2026-08-03),
  merged 2026-08-03T16:01:14Z. 10 findings (0 HIGH / 3 MED / 7 LOW),
  all with file:line evidence. MED: portability drops
  projects/spaces visibility+members on export/import round-trip;
  mixed-case mention handles never resolve; migration-guide documents
  non-existent `export-domain`/`import-domain` CLIs. Verified the
  `2ea7180` `.worktrees` guardrail fix is complete.
- #27 — audit-docs: documentation accuracy audit report (2026-08-03),
  merged 2026-08-03T16:01:18Z. 4 High / 7 Medium / 2 Low findings
  across `docs/` vs actual code/CLI (nonexistent CLI surface
  documented in migration-guide, false "no metrics endpoint" claims in
  metrics-dashboard/CHANGELOG, broken README archive links, stale
  phase-6/7/10 doc state). Verified: 332 tests pass, ruff clean, live
  migrate → export → import round-trip byte-identical.
- #28 — audit-sec: security and trust audit report (2026-08-03),
  merged 2026-08-03T16:01:21Z. 2 MED (audit sink never wired by
  CLI/serve; markdown importer reads full file before size check) and
  8 LOW. Clean axes: guardrail, secrets history, deps, append-only
  audit triggers, symlink/path-traversal blocking, YAML alias handling.

## Deferred

- #26 (csv-importer) — implementation still OPEN. QA gate passed
  (373 tests on the csv-importer branch vs 332 on main; coverage 84%
  at audit time); SEC gate and independent review still pending.
- #29 (audit-qa report) — review verdict REQUEST_CHANGES: 2 coverage
  overstatements. Corrections in progress on `feat/audit-qa`.
- #30 (consolidated audit report) — review verdict REQUEST_CHANGES:
  severity distribution misreported. Corrections in progress on
  `feat/audit-review`.

The P0 fix backlog (C1 = portability permission drop, C15 = mixed-case
mention handles) is not yet actionable as a fix task: it lives in the
consolidated report that is still under review (#30). Fix tasks will be
spawned from the backlog once that report clears review.

## Direction notes

- The audit sweep is the direction change this window: it converts the
  roadmap's "audit-export-flag / portability format" and "quality and
  operability" backlog items into concrete, prioritized findings with
  effort estimates, ready for the orchestrator to spawn fix tasks from.
- No changes to roadmap priorities, governance, or scope boundaries;
  `docs/roadmap.md` is unchanged. No beta issues filed; the graduation
  review described in `docs/post-launch-iteration.md` §6 remains open
  for a future tick.

## Late merges (addendum)

Appended after this note was finalised, per the append-only rule in
`docs/post-launch-iteration.md` §3. Two PRs merged moments after this
note was written — csv-importer (#26) at 2026-08-03T17:23:58Z and this
note itself (#31) at 2026-08-03T17:24:02Z — so the Landed list above
missed the first and the Deferred section recorded it as still open.
They are reconciled here so the record is complete.

### Landed (additional)

- #26 — feat(csv-importer): CSV/TSV importer for projects and work
  items (merged 2026-08-03T17:23:58Z, squash, branch deleted). The
  second post-launch backlog feature shipped: `innerwork import-csv` /
  TSV import for `projects` and `work_items`. Full chain landed: PM
  scoping (t_1ba7328a) → implementation (t_8609bbb9, 364 tests + 9 QA
  additions = 373 total) → QA gate (t_2ee06aae, PASS, coverage 99% on
  `csv_importer.py`, 41 importer tests) → SEC gate (t_965da436, 2
  low-severity findings: unbounded input size OOM and un-audited bulk
  import writes; no trust-boundary violations) → independent review
  (t_6ef9ca50, Verdict: approve, CI green 1 pass / 0 fail / 0 cancel).
- #31 — docs(iteration-notes): iteration note 2026-08-04 (merged
  2026-08-03T17:24:02Z). This note.

### Deferred (correction)

The Deferred section above listed csv-importer (#26) as "still OPEN";
it merged at 2026-08-03T17:23:58Z:

- #26 (csv-importer) — no longer deferred or in flight; SHIPPED in PR
  #26 (2026-08-03T17:23:58Z). Removed from the deferred list. 373 tests
  pass on the csv-importer branch vs 332 on main; coverage 84% overall.
- #29 (audit-qa report) — review verdict REQUEST_CHANGES (2 coverage
  overstatements); fix task t_78491af7 created on `feat/audit-qa`.
  Still in flight.
- #30 (consolidated audit report) — review verdict REQUEST_CHANGES
  (severity distribution misreported: 4 HIGH / 20 LOW vs the body's
  3 HIGH / 21 LOW); fix task t_c6a5d8b4 created on `feat/audit-review`.
  Still in flight.

The P0 fix backlog note above remains accurate: it is still not
actionable as a fix task because it lives in the consolidated report
(#30), which has not yet cleared review.

### Backlog loop

The post-launch backlog loop is now delivering end-to-end: markdown-
importer (#22) shipped earlier in the window, and csv-importer (#26)
cleared its full PM → eng → QA → SEC → independent-review chain,
merging minutes after the 2026-08-04 note was drafted. The two open
audit-report PRs (#29, #30) each have a fix task in flight. No beta-user
claims, no participant counts, and no roadmap-priority changes are
introduced by this addendum.

## Late merges (addendum 2)

Second append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. Three PRs merged after the first
addendum's window closed (2026-08-03T18:05–18:13Z) and are not covered
above: the audit-export-flag PM scoping (#34), the QA audit report
(#29), and the consolidated audit report (#30). The first addendum's
Deferred (correction) section recorded #29 and #30 as still in flight;
both have since cleared review and merged. They are reconciled here so
the record is complete.

### Landed (additional)

- #34 — docs(audit-export-flag): PM scoping for optional audit-log
  inclusion in portability payload (merged 2026-08-03T18:05:36Z, squash
  `789082f`, branch deleted). Converts the roadmap "Directional next" →
  Portability format item (`slug=audit-export-flag`) into a
  pre-implementation scoping doc,
  `docs/roadmap_audit_export_flag_scoping.md`, with an honest baseline
  table, an exact file-touch list (portability.py gains an optional
  `include_audit` path; cli.py gains `--include-audit` plus the audit-F1
  sink wiring; one test file, one fixture pair, one migration-guide
  section, one changelog entry), and strict scope boundaries for the
  implementation worker. Eng implementation task t_bb334626 is now
  running on `feat/audit-export-flag`; QA/SEC/review gates are queued
  behind it.
- #29 — docs(audit): test coverage and quality audit report (2026-08-03),
  merged 2026-08-03T18:13:32Z (squash, branch deleted). The last audit
  report from the sweep cleared review: REQUEST_CHANGES on 2 coverage
  overstatements → fix task t_78491af7 corrected the report on
  `feat/audit-qa` (commit `823d92c`) → re-review t_25da2f7e returned
  Verdict: approve, CI 1 pass / 0 fail / 0 cancel → orchestrator merged.
  Report at `docs/audit/2026-08-03-qa.md`: baseline 332 passed in 68.71s,
  84% total coverage, ruff clean, pyright 0 errors; findings 3 High /
  5 Medium / 4 Low plus §3.3 (guardrail fix `2ea7180` has no regression
  test).
- #30 — docs(audit): consolidated audit report + prioritized fix backlog
  (merged 2026-08-03T18:13:48Z, squash, branch deleted). The
  consolidation cleared review after a severity-distribution correction:
  REQUEST_CHANGES (report labels 4 HIGH / 20 LOW vs the PR body's
  3 HIGH / 21 LOW) → fix task t_c6a5d8b4 corrected the report on
  `feat/audit-review` (head `943fade`) → re-review t_cbd81421 returned
  Verdict: approve → orchestrator merged. Report at
  `docs/audit/2026-08-03-consolidated.md`: 38 consolidated findings →
  0 CRITICAL / 4 HIGH / 14 MED / 20 LOW; the four HIGHs are C1
  (portability drops `visibility`/`members`), C21 (migration-guide
  documents a nonexistent CLI surface), C22 (false metrics/telemetry
  claims), and C32 (`ai_context.py` security-adjacent paths with zero
  coverage). The report ends with the prioritized fix backlog (P0–P3)
  the orchestrator can spawn fix tasks from.

### Deferred (correction)

The first addendum's Deferred (correction) section listed #29 and #30 as
still in flight; both merged moments later and are no longer deferred:

- #29 (audit-qa report) — SHIPPED in PR #29 (2026-08-03T18:13:32Z).
  Removed from the in-flight list.
- #30 (consolidated audit report) — SHIPPED in PR #30
  (2026-08-03T18:13:48Z). Removed from the in-flight list.

The P0 fix backlog note above is now actionable: it lives in the
consolidated report (#30), which has cleared review, so the orchestrator
can spawn fix tasks from the C1/C15 items. The first audit-derived
implementation task to start from the backlog loop is t_bb334626
(audit-export-flag, scoped in #34), with QA/SEC/review gates queued
behind it. No beta-user claims, no participant counts, and no
roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 3)

Third append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after addendum 2's
window closed: the audit-export-flag implementation (#36) at
2026-08-03T19:53:50Z. Addendum 2 recorded its scoping (#34) and the
in-flight implementation task t_bb334626; the implementation has since
cleared QA, SEC, and independent review and merged. It is reconciled
here so the record is complete.

### Landed (additional)

- #36 — feat(portability): opt-in audit-log inclusion in export (merged
  2026-08-03T19:53:50Z, squash `5b1bc3da`, branch deleted). Implements
  the scoping doc from #34 (`docs/roadmap_audit_export_flag_scoping.md`)
  on `feat/audit-export-flag`: `export --include-audit` plus `--audit-log`
  on export/import wire the audit sink; `format_version` bumps to 2
  **only** when the audit collection is present, and default exports
  stay byte-identical v1 (fixture-locked); import validates every row
  via strict `make_event` reconstruction (closed enums, no event
  injection), all-or-nothing `event_id` conflict pre-check, and restores
  rows to the sink append-only after domain inserts. Also fixes audit
  finding F1: the CLI now wires a sink on all domain subcommands, so
  shipped writes emit audit rows. Gates: QA t_a2fb9fa8 (portability.py
  98% coverage; 405 tests = 373 pre-existing + 32 new; ruff clean;
  pyright 0 errors), SEC t_b1939080 (completed without a live run —
  spawn-failed twice on a worktree conflict; its checklist was covered
  by the reviewer per that summary), independent review t_37c04af7
  Verdict: approve with CI green.

### Deferred (correction)

The 2026-08-03 note's Deferred section listed audit-export-flag under
"Portability format" as a roadmap backlog item; #34 scoped it and #36
implemented it. It is no longer deferred:

- audit-export-flag — SHIPPED in PR #36 (2026-08-03T19:53:50Z).
  Removed from the deferred list; the roadmap bullet moved to shipped.
- streaming-export for very large stores remains deferred under
  Portability format.

### Direction notes

- `docs/roadmap.md` is no longer unchanged: #36 moved the
  audit-export-flag bullet from "Directional next → Portability format"
  to the shipped list — the first roadmap content change since the
  audit sweep. The remaining Portability format item is
  streaming-export.
- The backlog loop has now graduated a full PM-scoping → implementation
  → QA → SEC → review chain for a portability-format item
  (audit-export-flag via #34 → #36). The C1 portability permission drop
  and C15 mixed-case mention handles remain open fix items from the
  consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 4)

Fourth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. Two docs-only PRs for the
streaming-export roadmap item landed after addendum 3 was drafted: the
PM scoping (#38) at 2026-08-03T20:22:03Z and its post-implementation
amend (#40) at 2026-08-03T20:29:12Z. Addendum 3 (PR #37, merged
21:03:44Z) merged after both, but its Deferred (correction) section —
written before they landed — still recorded streaming-export as
deferred under Portability format. They are reconciled here so the
record is complete.

### Landed (additional)

- #38 — docs(streaming-export): PM scoping for streaming export of
  very large stores (merged 2026-08-03T20:22:03Z, `cac1ff8`). Converts
  the roadmap "Directional next" → Portability format item
  (`slug=streaming-export`) into a pre-implementation scoping doc,
  `docs/roadmap_streaming_export_scoping.md`: honest baseline table
  (today's `export` is memory-resident — `export_domain_json` builds
  the full string, then `write_text`/stdout; no streaming primitives
  anywhere), locked design (additive
  `export_domain_json_stream(store, out, ...)` writer, byte-identical
  to `export_domain_json`, peak memory O(batch_size × row) via
  `fetchmany` — never `fetchall`; atomic `--out` temp+rename; stdout
  streams; import side out of scope), exact file-touch list, and §9
  exit criteria for the implementation worker.
- #40 — docs(streaming-export): reconcile scoping with child task,
  progress flag, measurement plan (merged 2026-08-03T20:29:12Z,
  `dbab9b1`). Amends the scoping doc after implementation task
  t_93948c32 started: adds `--progress` as the only new CLI flag
  (stderr progress lines at a documented cadence — collection start,
  every 100 000 rows, collection done, final; constant collection
  names + integers only, never row content; silent without the flag),
  a `progress` callback parameter on `export_domain_json_stream`, and
  a measurement plan for the memory bound (tracemalloc differential on
  a 40k-row seeded store: memory-resident peak ≥ 8 MiB floor guard,
  streamed peak < 25% of that — target vs measured kept distinct, no
  absolute MB/GB figures). Reconciles the child-task reference.

### In flight

- streaming-export implementation — PR #41
  `feat(portability): streaming export for very large stores
  (export_domain_json_stream, atomic --out)` is OPEN (branch
  `feat/streaming-export`, head `59a174f`, opened 2026-08-03T20:46:39Z,
  CI `test` check SUCCESS, mergeable). Implements the scoping doc:
  `export_domain_json_stream` (batch_size=500, byte-identical output,
  audit/v2 composition, fail-before-write, portability event with real
  counts only after a fully successful write), atomic `--out` CLI, 22
  new tests in `tests/test_portability_stream.py` (427 local tests
  pass, ruff clean, pyright 0 errors), migration-guide §2/§6 notes,
  CHANGELOG entry. Per the PR body it deliberately omits the scoping
  amend's `--progress` flag — not in the locked card t_93948c32, so
  `--help` is unchanged. QA gate t_28c735b3 is running; SEC gate
  t_6a07849e and independent review t_6ba7d267 are queued behind it.
  NOT shipped: `docs/roadmap.md` on `main` still carries the bullet
  under "Directional next" → Portability format (PR #41's branch moves
  it, but that change is not merged).

### Deferred (correction)

Addendum 3's Deferred (correction) listed "streaming-export for very
large stores remains deferred under Portability format". It is no
longer deferred:

- streaming-export — no longer deferred; IN FLIGHT. PM scoping landed
  (#38) and was amended post-implementation-start (#40); implementation
  PR #41 is open under QA gate t_28c735b3, with SEC gate t_6a07849e and
  independent review t_6ba7d267 queued behind it. Not shipped.

### Direction notes

- streaming-export is the second Portability format item to enter the
  backlog loop after audit-export-flag (#34 → #36, shipped in addendum
  3): scoping #38/#40 → implementation PR #41 → gates in progress.
- `docs/roadmap.md` on `main` is unchanged by this window's merges:
  the streaming-export bullet remains under "Directional next" →
  Portability format until the implementation lands.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 5)

Fifth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after addendum 4's
window closed: the streaming-export implementation (#41) at
2026-08-03T23:36:01Z. Addendum 4 recorded the PM scoping (#38/#40) and
the implementation PR #41 as OPEN under its QA gate; the
implementation has since cleared QA, SEC, and independent review and
merged. It is reconciled here so the record is complete.

### Landed (additional)

- #41 — feat(portability): streaming export for very large stores
  (merged 2026-08-03T23:36:01Z, squash `302d9da`, branch deleted).
  Implements the scoping doc from #38/#40
  (`docs/roadmap_streaming_export_scoping.md`) on
  `feat/streaming-export` (head `4276188`):
  `export_domain_json_stream(store, out, *, indent=2, batch_size=500,
  include_audit=False, audit_actor_kind="system", progress=None)`
  writes the portability envelope incrementally via
  `fetchmany(batch_size)` batches — never `fetchall` — so peak
  additional memory is O(batch_size × row) instead of O(store);
  output is byte-identical to the memory-resident
  `json.dumps(export_domain(...))` (fixture-locked referee incl.
  adversarial unicode/control chars, compact `indent=None`,
  multi-batch, `batch_size=1`, empty store). Audit composition keeps
  `format_version` 2 and redaction parity via the existing
  `_export_audit_rows`; the sink-missing check fires before the first
  byte (fail-before-write), and `_audit_portability` records real
  counts + effective format_version only after a fully successful
  write (an interrupted export records no event). CLI `export` now
  always streams: `--out` is atomic (temp `<pid>` file +
  `os.replace`, temp removed on every failure path, existing target
  never clobbered), otherwise stdout; `--progress` (QA-reconciled, see
  correction below) emits stderr cadence lines of constant collection
  names + integers only — never row content — and stderr is silent
  without the flag. 30 new tests in
  `tests/test_portability_stream.py`. Gates: QA t_28c735b3 (435 passed
  = 405 pre-existing + 30 new, none modified;
  `src/innerwork/portability.py` 99% coverage with the new streaming
  path fully covered; all 5 PM acceptance criteria MET; measured
  tracemalloc differential on a 40k-row store — streamed peak 0.79 MiB
  vs 112.5 MiB memory-resident = 0.7%, under the < 25% target, with a
  ≥ 8 MiB floor guard), SEC t_6a07849e (PASS, no findings:
  threat-model conformance with redaction parity verified on the
  streamed artifact, zero dependency changes, secrets clean,
  guardrail check OK, malformed/truncated re-import exits 2 without
  crash), independent review t_6ba7d267 (Verdict: approve; CI green 1
  pass / 0 fail / 0 cancel; local parity 435 passed, ruff clean,
  pyright 0 errors). Roadmap bullet moved to "Shipped through Phase
  10".

### Deferred (correction)

Addendum 4's "In flight" section recorded the streaming-export
implementation PR #41 as OPEN under QA gate t_28c735b3 with SEC gate
t_6a07849e and independent review t_6ba7d267 queued behind it; it also
noted — per the PR body at that time — that the implementation
"deliberately omits the scoping amend's `--progress` flag". Both are
superseded:

- streaming-export — no longer in flight; SHIPPED in PR #41
  (2026-08-03T23:36:01Z). Removed from the in-flight list; the roadmap
  bullet now sits under "Shipped through Phase 10" on `main`.
- `--progress` — ADDED during the QA gate, not omitted. QA gate
  t_28c735b3 found PM acceptance criterion 5 unmet: the scoping doc
  (#38/#40) locks `--progress` as the single new flag (§2/§3.1/§9),
  and the implementation card's "No new flags" line contradicted the
  PM's own amendment. QA added the flag, the `progress` callback on
  `export_domain_json_stream`, and 5 tests on the branch (head
  `4276188`); `--help` documents it.

### Direction notes

- streaming-export is the third Portability format item to complete
  the full PM-scoping → implementation → QA → SEC → review backlog
  loop after audit-export-flag (#34 → #36, addendum 3): scoping
  #38/#40 → implementation PR #41 → gates all green → merged. The
  Portability format section of `docs/roadmap.md` now holds only the
  compact (`indent=None`) streaming plumbing candidate (directional,
  no commitment).
- `docs/roadmap.md` on `main` is no longer unchanged by this window's
  merges: #41 moved the streaming-export bullet to "Shipped through
  Phase 10" — the second roadmap content change since the audit
  sweep, after #36's audit-export-flag move (addendum 3).
- With streaming-export shipped, the next roadmap item in line is the
  "Quality and operability" candidate "Expand `innerwork metrics`
  output with optional time-windowed aggregations" (time-windowed-
  metrics; the rollup is point-in-time only today) — the
  orchestrator's next candidate alongside the C1/C15 fix backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 6)

Sixth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after addendum 5's
window closed: the time-windowed-metrics PM scoping (#44) at
2026-08-03T23:43:08Z. Addendum 5's Direction notes flagged the
"Quality and operability" candidate "Expand `innerwork metrics` output
with optional time-windowed aggregations" as the orchestrator's next
candidate; the scoping has since landed and its implementation PR is
open with its QA gate passed. Both are reconciled here so the record
is complete.

### Landed (additional)

- #44 — docs(time-windowed-metrics): PM scoping for optional windowed
  metrics aggregations (merged 2026-08-03T23:43:08Z, squash `167a766`,
  branch deleted). Converts the roadmap "Quality and operability"
  candidate `slug=time-windowed-metrics` (PM task t_a3e75717) into a
  pre-implementation scoping doc,
  `docs/roadmap_time_windowed_metrics_scoping.md` (254 lines, 11
  sections, grounded against main @ `302d9da`): honest baseline (the
  `innerwork metrics` rollup is point-in-time only today), locked
  design — additive `windowed_domain_rollup` with
  `DomainWindowMetrics` / `ProjectCycleTime` / `PageWritesRollup` /
  `ContributorsRollup` in `analytics.py` (~+150 lines) — exact
  file-touch list (`analytics.py`, `cli.py` +2 flags, new
  `tests/test_analytics_windowed.py`, `docs/metrics-dashboard.md`,
  `CHANGELOG`), CLI surface `--window-start`/`--window-end`
  (ISO-8601, offset required; default output byte-identical with no
  `window` key), and exit criteria for the implementation worker.

### In flight

- time-windowed-metrics implementation — PR #45 `feat(analytics):
  optional time-windowed aggregations for innerwork metrics
  (--window-start/--window-end)` is OPEN (branch
  `feat/time-windowed-metrics`, head `88c2810`, opened
  2026-08-03T23:55:11Z, CI `test` check SUCCESS, mergeable; DO NOT
  MERGE per the eng task). Eng task t_4faf0860 implemented the scoping
  doc on the §1 boundary and blocked as qa-required:
  `windowed_domain_rollup` plus the four windowed models in
  `analytics.py`, `--window-start`/`--window-end` on `innerwork
  metrics` (`AnalyticsError` → exit 2), 17 new tests in
  `tests/test_analytics_windowed.py` (452 passed at handoff, ruff
  clean, pyright 0 errors, no new deps), metrics-dashboard §4 +
  CHANGELOG entry; `docs/roadmap.md` bullet moved to shipped on the
  branch only. QA gate t_1bb51dc2 PASSED: 454 passed (452 pre-existing
  + 2 new QA regression tests, 0 failed); coverage 89% total (4146
  stmts) / `analytics.py` 99% / new windowed module
  (`analytics.py:267-563`) 100% in-process; all PM acceptance criteria
  MET (default byte-identical, hand-computed fixture aggregations,
  zero-activity windows → explicit zeros, CLI help documents flags).
  QA closed two coverage gaps on the branch (committed `88c2810`): the
  page-comment-out-of-window branch (`analytics.py:520`) had zero
  tests, and 3 flag-validation branches were only exercised via CLI
  subprocess (invisible to pytest-cov) — added in-process assertions.
  Security/edge cases verified: malformed dates exit 2 with no
  traceback, inverted/zero-length windows rejected (`end <= start`),
  naive timestamps rejected, half-open `[start, end)` boundary,
  permission model unchanged (same `can_read` gate as `domain_rollup`,
  no new data exposure). QA blocked as sec-required; security audit
  t_5bc89ffe is RUNNING on `feat/time-windowed-metrics`; independent
  review t_89f4ad8f is queued behind it. NOT shipped:
  `docs/roadmap.md` on `main` still carries the bullet under "Quality
  and operability".

### Direction notes

- time-windowed-metrics is the first roadmap item from the "Quality
  and operability" section to run the PM-scoping → implementation →
  QA → SEC → review backlog loop (the two portability-format items,
  audit-export-flag #34 → #36 and streaming-export #38/#40 → #41,
  completed it earlier in addenda 3 and 5). The C1 portability
  permission drop and C15 mixed-case mention handles remain open fix
  items from the consolidated backlog.
- `docs/roadmap.md` on `main` is unchanged by this window's merges:
  #44 is docs-only; #45's branch moves the "Quality and operability"
  bullet to shipped, but that change is not merged.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 7)

Seventh append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. Two PRs merged after addendum 6's
window closed: the time-windowed-metrics implementation (#45) at
2026-08-04T04:27:36Z and the backup-restore-upgrade PM scoping (#47) at
2026-08-04T04:38:33Z. Addendum 6 recorded the time-windowed-metrics
scoping (#44) and listed implementation PR #45 as OPEN under its QA
gate with SEC and independent review queued; the implementation has
since cleared all three gates and merged — nine minutes before
addendum 6 itself merged. The backup-restore-upgrade chain that #45
unblocks has also started: its PM scoping (#47) landed two minutes
after addendum 6. Both are reconciled here so the record is complete.

### Landed (additional)

- #45 — feat(analytics): optional time-windowed aggregations for
  innerwork metrics (--window-start/--window-end) (merged
  2026-08-04T04:27:36Z, squash `90eae89`, branch deleted). Implements
  the scoping doc from #44 (`docs/roadmap_time_windowed_metrics_scoping.md`)
  on `feat/time-windowed-metrics` (head `88c2810`):
  `windowed_domain_rollup(store, *, window_start=None, window_end=None,
  principal=None)` returns `DomainWindowMetrics` (`to_dict()` → top-level
  `"window"` object) plus helper dataclasses `ProjectCycleTime` /
  `PageWritesRollup` / `ContributorsRollup` in `analytics.py` (~+300
  lines, all additive; `domain_rollup`/`project_rollup`/`space_rollup`
  bodies untouched). Four activity-over-window rollups over half-open
  `[start, end)` UTC windows: `state_counts` (transitions into each
  state), `cycle_time_per_project` (done-transition cycle times,
  count/avg/min/max, null when 0), `page_writes` (versions/touched/
  by_space from `page_versions`), `contributors` (distinct/by_actor).
  CLI `metrics` gains `--window-start`/`--window-end` (ISO-8601 with
  explicit UTC offset; naive rejected); no flags → `domain_rollup`
  byte-identical as today; `AnalyticsError` → `error: <msg>` stderr +
  exit 2, empty stdout, no partial JSON. 19 new tests in
  `tests/test_analytics_windowed.py` (17 eng + 2 QA regression) —
  hand-computed literals, byte-identity of the default path,
  zero-activity explicit zeros, `[start, end)` boundary semantics,
  partial windows, timezone equivalence, naive-stored-as-UTC, flag
  validation (exit 2 / no traceback / no partial JSON), loud
  unparseable-stored failure, permission filtering, synthetic-fixture
  full-window oracle, `--help` docs, determinism, no-new-dependencies.
  `docs/metrics-dashboard.md` gains §4 (windowed shape/semantics/
  boundaries/permission note + the opt-in exception to the
  no-user-identifiers doctrine) and amends the §2 "No time series"
  bullet; CHANGELOG entry under [Unreleased]; roadmap bullet moved to
  "Shipped through Phase 10". Gates: QA t_1bb51dc2 PASSED — 454 passed
  (452 pre-existing + 2 new QA regression tests at `88c2810`, 0
  failed); coverage 89% total (4146 stmts) / `analytics.py` 99% /
  windowed module (`analytics.py:267-563`) 100% in-process; all 4 PM
  acceptance criteria MET (default byte-identical, hand-computed
  fixture aggregations, zero-activity windows → explicit zeros, CLI
  help documents flags). QA closed two in-process coverage gaps
  (committed `88c2810`): the page-comment-out-of-window branch
  (`analytics.py:520`) had zero tests, and 3 flag-validation branches
  were only exercised via CLI subprocess (invisible to pytest-cov).
  SEC t_5bc89ffe PASS — no must-fix findings, 5/5 checklist:
  threat-model conformance (no new trust boundary/write surface;
  `windowed_domain_rollup` reuses `_project_readable`/`_space_readable`
  gates; permission filtering covered by `test_permission_filtering`),
  zero dependency diff (`uv pip check`: 31 packages compatible; CI
  guard `test_no_new_dependencies` present), secrets clean (token
  grep zero hits, `.env` untouched), guardrail check OK
  (`scripts/check_anti_hallucination.py` exit 0), input safety
  (malformed/naive/inverted/zero-length windows → exit 2, no
  traceback, no eval/exec, no subprocess, static SQL). 3 informational
  notes: N1 `contributors.by_actor` emits actor identifiers (opt-in,
  readability-gated, documented as the §4 opt-in exception to the
  no-user-identifiers doctrine), N2 loud failure on unparseable stored
  timestamps by design (never silently undercounts), N3 O(N)
  Python-side filtering by design (arbitrary ISO offsets make SQL-side
  lexicographic comparison unsound). Independent review t_89f4ad8f —
  Verdict: approve; CI green (1 check `test` SUCCESS,
  actions/runs/30864536450); local parity 454 passed, ruff clean,
  pyright 0 errors, OpenAPI contract valid; anti-hallucination check
  OK. Non-blocking nit: CHANGELOG test count says 17, final head has
  19 (commit 2 added the 2 QA regression tests without bumping the
  count). Orchestrator merged via admin squash (the self-approval rule
  blocked `gh pr review --approve` for the m0n3r0-authored PR).
- #47 — docs(backup-restore-upgrade): PM scoping for explicit
  backup/restore/upgrade runbook procedures (merged 2026-08-04T04:38:33Z,
  squash `6ae2ee3`, branch deleted). Converts the roadmap "Quality and
  operability" candidate "Tighten the operations runbook with explicit
  backup / restore / upgrade procedures" (`slug=backup-restore-upgrade`,
  PM task t_4df57803) into a pre-implementation scoping doc,
  `docs/roadmap_backup_restore_upgrade_scoping.md` (184 lines),
  grounded on main @ `939e75e`. Key finding: documentation-only slice —
  `scripts/backup.py` (online-backup API, chmod 0o600),
  `scripts/restore.py` (--force guard), and `scripts/rollback_drill.py`
  already ship and are tested (`tests/test_backup_restore.py`,
  `tests/test_rollback_drill.py`); implementation touches exactly
  `docs/operations-runbook.md` (add Backup/Restore/Upgrade sections),
  `docs/roadmap.md` (move bullet to Shipped), `CHANGELOG.md` — no code,
  no scripts, no new tests. All runbook commands verified live
  (evidence matrix in scoping §8: backup→wipe→restore→semantic parity,
  export/import round-trip format_version 1 / schema_version 4,
  audit-store backup, rollback drill ok:true, `migrate --source
  synthetic`). Honest gap calls locked: no production-store restore
  drill recorded (CI-gated automated drill exists); audit sink
  CLI-gated (`innerwork serve` does not wire one); no automated
  upgrade path beyond the portability envelope; `sqlite3` CLI optional —
  stdlib Python one-liner is the default verification. Implementation
  task t_9ab6c855 is spawned with QA t_cd77f760 / SEC t_642c9c49 /
  independent review t_81b0d740 queued behind it.

### Deferred (correction)

Addendum 6's "In flight" section recorded the time-windowed-metrics
implementation PR #45 as OPEN (QA gate passed, SEC running, review
queued) and its Direction notes asserted `docs/roadmap.md` on `main`
was unchanged because "#45's branch moves the bullet to shipped, but
that change is not merged". Both are superseded:

- time-windowed-metrics — no longer in flight; SHIPPED in PR #45
  (2026-08-04T04:27:36Z). Removed from the in-flight list; the
  "Quality and operability" bullet now sits under "Shipped through
  Phase 10" on `main`.
- `docs/roadmap.md` on `main` is no longer unchanged: #45 moved the
  time-windowed-metrics bullet to shipped — the third roadmap content
  change since the audit sweep, after #36's audit-export-flag move
  (addendum 3) and #41's streaming-export move (addendum 5). #47 is
  docs-only; the backup-restore-upgrade bullet remains under
  "Directional next" → Quality and operability until its
  implementation lands.

### Direction notes

- time-windowed-metrics is the first roadmap item from the "Quality
  and operability" section to complete the full PM-scoping →
  implementation → QA → SEC → review backlog loop (the two
  portability-format items, audit-export-flag #34 → #36 and
  streaming-export #38/#40 → #41, completed it earlier in addenda 3
  and 5). The C1 portability permission drop and C15 mixed-case
  mention handles remain open fix items from the consolidated
  backlog.
- What #45 unblocks: the next "Quality and operability" candidate,
  backup-restore-upgrade, entered the loop the same window. Its PM
  scoping (#47) merged two minutes after addendum 6, and the
  implementation chain is spawned: t_9ab6c855 (impl, child of
  t_4df57803) with QA t_cd77f760, SEC t_642c9c49, and independent
  review t_81b0d740 queued behind it.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 8)

Eighth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. Two PRs merged after addendum 7's
window closed — four seconds apart: the backup-restore-upgrade
implementation (#49) at 2026-08-04T06:46:40Z and addendum 7 itself
(#48) at 2026-08-04T06:46:44Z. Addendum 7 recorded the
backup-restore-upgrade PM scoping (#47) and listed the implementation
chain as spawned with QA/SEC/review queued behind it; the
implementation has since cleared all three gates and merged — before
addendum 7's own squash landed, which is why that note still described
the chain as queued. Both are reconciled here so the record is
complete.

### Landed (additional)

- #49 — docs(backup-restore-upgrade): locked Backup/Restore/Upgrade
  procedures in the operations runbook (merged 2026-08-04T06:46:40Z,
  squash `0ce74cb`, branch `feat/backup-restore-upgrade` deleted).
  Implements the scoping doc from #47
  (`docs/roadmap_backup_restore_upgrade_scoping.md`) and closes
  roadmap backlog item #6 end-to-end: PM scoping (#47) →
  implementation (#49) → QA gate passed (457 tests, 89% coverage) →
  SEC gate passed (no findings) → independent review approved →
  merged. `docs/operations-runbook.md`'s thin "Backup & restore"
  section restructured into three locked, copy-paste sections:
  Backup (domain store, audit store if enabled, env-var config;
  online-backup snapshots via `scripts/backup.py`, chmod 0o600; no
  off-host integration ships; retention is operator guidance),
  Restore (stop service → `scripts/restore.py` → integrity check via
  the stdlib Python one-liner — `sqlite3` CLI optional → real-CLI
  data verification → optional export round-trip; honest gap call: no
  production-store restore drill recorded), and Upgrade (version
  check; the portability envelope as the only data-migration path,
  schema_version/format_version import gate; pre-upgrade backup
  requirement; rollback plan; no automated upgrade path). ToC +
  incident-playbook cross-reference updated. `docs/roadmap.md`: the
  bullet moves from "Directional next → Quality and operability" to
  "Shipped through Phase 10" as a post-phase-10 addition, and the
  drifted `export-domain`/`import-domain` CLI names are corrected to
  `export`/`import`. CHANGELOG [Unreleased] entry — docs-only slice,
  no version bump. QA gate t_cd77f760 PASSED — exercising every
  documented command against ephemeral stores surfaced two
  pre-existing script defects, fixed on the branch: `scripts/backup.py`
  now unlinks a stale/partial destination before the online-backup
  copy (previously blocked with "file is not a database",
  contradicting the runbook's overwrite guarantee), and
  `scripts/restore.py` now copies into a sibling temp file and
  atomically renames into place only on success (previously `--force`
  unlinked the existing destination before copying, so a corrupt or
  truncated backup destroyed the store it was replacing). Three new
  tests in `tests/test_backup_restore.py` (stale-dest overwrite,
  corrupt-backup leaves no dest, failed `--force` preserves existing
  dest); runbook Restore section amended with the atomic-restore
  guarantee. CI parity: pytest 457 passed, ruff clean, pyright 0
  errors; coverage innerwork 89% overall, portability 99%,
  domain_store 94%. SEC t_642c9c49 PASS — no findings. Independent
  review t_81b0d740 — Verdict: approve. Merged via squash, branch
  deleted.
- #48 — docs(iteration-notes): addendum 7 to 2026-08-04 note for
  time-windowed-metrics implementation merge (#45) (merged
  2026-08-04T06:46:44Z, squash `b789e37`, branch
  `feat/iteration-note` deleted). The note immediately preceding this
  one. Its PR merged after a follow-up fix on the branch: commit
  `f836b41` corrected the PM acceptance-criteria count in the #45 QA
  paragraph from 5 to 4 (the scoping doc locks 4 criteria). Because
  it merged four seconds after #49, its Deferred (correction) and
  Direction notes still record the backup-restore-upgrade
  implementation as queued — superseded below.

### Deferred (correction)

Addendum 7's Direction notes recorded the backup-restore-upgrade
implementation chain as spawned (t_9ab6c855, child of t_4df57803)
with QA t_cd77f760 / SEC t_642c9c49 / independent review t_81b0d740
queued behind it, and its Deferred (correction) section asserted
`docs/roadmap.md` on `main` was unchanged because "the
backup-restore-upgrade bullet remains under 'Directional next' →
Quality and operability until its implementation lands". Both are
superseded:

- backup-restore-upgrade — no longer in flight; SHIPPED in PR #49
  (2026-08-04T06:46:40Z), four seconds before addendum 7 itself
  merged. The full chain completed (PM scoping #47 → implementation
  #49 → QA 457 tests / 89% coverage → SEC no findings → independent
  review approve), closing roadmap backlog item #6 end-to-end; the
  bullet now sits under "Shipped through Phase 10" on `main`.
- `docs/roadmap.md` on `main` is no longer unchanged: #49 moved the
  backup-restore-upgrade bullet to shipped — the fourth roadmap
  content change since the audit sweep, after #36's audit-export-flag
  move (addendum 3), #41's streaming-export move (addendum 5), and
  #45's time-windowed-metrics move (addendum 7) — and also corrected
  the drifted `export-domain`/`import-domain` CLI names to
  `export`/`import`.

### Direction notes

- backup-restore-upgrade is the second roadmap item from the
  "Quality and operability" section to complete the full PM-scoping →
  implementation → QA → SEC → review backlog loop, after
  time-windowed-metrics #44 → #45 (addendum 7). The C1 portability
  permission drop and C15 mixed-case mention handles remain open fix
  items from the consolidated backlog.
- The "Quality and operability" section of `docs/roadmap.md` now
  holds a single remaining candidate: documenting a recommended
  Prometheus / log-scraping shape for operators.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 9)

Ninth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after addendum 8's
window closed: the observability-shape documentation (#51) at
2026-08-04T08:36:51Z. Addendum 8's Direction notes flagged the last
remaining "Quality and operability" candidate — documenting a
recommended Prometheus / log-scraping shape for operators; it has
since cleared its full docs-task → review → follow-up → re-review
chain and merged, completing roadmap slug `observability-shape`. It
is reconciled here so the record is complete.

### Landed (additional)

- #51 — docs(observability-shape): recommended Prometheus / log-scraping
  shape documentation (merged 2026-08-04T08:36:51Z, squash `3284532`,
  branch `feat/observability-shape` deleted). Converts the roadmap
  "Quality and operability" candidate `slug=observability-shape` into
  the new `docs/observability-shape.md` (318 lines). Shipped surface —
  `GET /metrics` serves the in-process registry as Prometheus 0.0.4
  text on the API port (`src/innerwork/app.py:172`,
  `include_in_schema=False`): full metric catalog
  (`http_requests_total`, `http_request_errors_total`,
  `http_request_duration_ms`, `domain_writes_total`,
  `domain_write_conflicts_total`, `span_duration_ms` — the two domain
  counters declared but never incremented), label semantics
  (route-template `endpoint` cardinality control; `reason` =
  `"exception"` or `status_<code>` such as `status_500`; `status` =
  `str(status_code)`; `span` = trace-span name) and ms histogram
  buckets (5..5000) — plus the JSON-lines log shape
  (`ts`/`level`/`logger`/`msg`/`request_id`/`spans`/`exc`/`extra`,
  `x-request-id` read/mint ≤128 chars and echoed). The rest is
  explicitly-labeled recommendations: an `innerwork_*` naming/label
  convention for future domain/workflow metrics (bounded cardinality,
  worked `innerwork_transitions_total` example), a scrape-friendly log
  contract, and collection guidance with an honest tradeoff table —
  pull-based scrape of the existing `/metrics` endpoint (scrape_config
  example) vs textfile-collector / pushgateway for the `innerwork
  metrics` CLI rollup (rollup-JSON → metric mapping table,
  `cycle_time_*_seconds` units, `by_space`/`by_actor` cardinality
  caveat). No exporter ships — the doc states plainly that nothing
  beyond the in-process registry and the `/metrics` route exists and
  every collection pattern is operator-side. Also changed:
  `docs/roadmap.md` — the bullet moves from "Directional next → Quality
  and operability" to "Shipped through Phase 10" (post-phase-10
  addition) and the now-empty "Quality and operability" section header
  is removed from "Directional next"; `CHANGELOG.md` — [Unreleased]
  docs-only entry, no version bump; `docs/site-outline.md` — new doc in
  the Operations nav + IA bucket. Chain: docs task t_00b7477c opened
  the PR (head `b16ad59`); review t_20934411 returned REQUEST_CHANGES
  on one warning-level factual error — §1.2 claimed the `reason` label
  value is the literal `status_5xx`, but `src/innerwork/app.py:151`
  emits `f"status_{status_code}"` and the literal never appears in any
  emitted label (a PromQL selector on `reason="status_5xx"` would
  silently match nothing) — follow-up t_05272189 fixed it on the SAME
  branch (commit `bad1763`, docs-only 2-line change to
  `docs/observability-shape.md`); re-review t_f6eb5302 returned
  Verdict: approve (CI green 1 pass / 0 fail / 0 cancel on check
  `test`, actions/runs/30885968228; local parity 457 passed, ruff
  clean, pyright 0 errors, anti-hallucination OK; no `status_5xx`
  literal anywhere in src/tests/docs). Orchestrator merged via admin
  squash — `gh pr merge 51 --squash --delete-branch --admin` (the
  self-approval rule blocked `gh pr review --approve` for the
  m0n3r0-authored PR).

### Deferred (correction)

Addendum 8's Direction notes said the "Quality and operability"
section of `docs/roadmap.md` now holds a single remaining candidate:
documenting a recommended Prometheus / log-scraping shape. Superseded:

- observability-shape — no longer a candidate; SHIPPED in PR #51
  (2026-08-04T08:36:51Z). The bullet now sits under "Shipped through
  Phase 10" on `main`, and the section header itself was removed from
  "Directional next" — the section holds no candidates at all now
  (time-windowed-metrics and backup-restore-upgrade shipped earlier in
  addenda 7 and 8).

### Direction notes

- observability-shape is the third roadmap item from the "Quality and
  operability" section to complete the backlog loop, after
  time-windowed-metrics #44 → #45 (addendum 7) and
  backup-restore-upgrade #47 → #49 (addendum 8) — and with it that
  section is complete: all three candidates shipped and the section
  header is gone from `docs/roadmap.md`. It is also the first roadmap
  item whose chain ran docs task → review → follow-up → re-review with
  no separate PM-scoping or implementation card (the docs task itself
  was the deliverable).
- `docs/roadmap.md` on `main` changed again: #51 moved the
  observability-shape bullet to shipped — the fifth roadmap content
  change since the audit sweep, after #36's audit-export-flag move
  (addendum 3), #41's streaming-export move (addendum 5), #45's
  time-windowed-metrics move (addendum 7), and #49's
  backup-restore-upgrade move (addendum 8) — and removed the emptied
  section header. The C1 portability permission drop and C15
  mixed-case mention handles remain open fix items from the
  consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 11)

Eleventh append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
the note section immediately above: PR #53 — docs(iteration-notes):
addendum 9 to the 2026-08-04 note for the observability-shape docs
merge (#51) — at 2026-08-04T09:30:33Z. Addendum 10 (PR #54), covering
the innerwork-doctor scoping merge #52, was still in flight on
`feat/iteration-note-2026-08-04-addendum-10` when this addendum was
drafted and referenced PR #53 as in-flight; that reference is now
superseded — PR #53 has merged, so this addendum follows addendum 9's
content on `main` and records PR #54 as still in flight. It is
reconciled here so the record is complete.

### Landed (additional)

- #53 — docs(iteration-notes): addendum 9 to 2026-08-04 note for
  observability-shape docs merge (#51) (merged 2026-08-04T09:30:33Z,
  squash `2b3d425`, branch `feat/iteration-note` deleted). The note
  section immediately preceding this one, merged by the orchestrator
  via admin squash (the self-approval rule blocked `gh pr review
  --approve` for the m0n3r0-authored PR). It appends addendum 9 to
  this file per the append-only rule in
  `docs/post-launch-iteration.md` §3, covering the observability-shape
  documentation (#51, merged 2026-08-04T08:36:51Z, squash `3284532`,
  branch `feat/observability-shape` deleted) — the third and final
  "Quality and operability" roadmap item to ship, via its docs task →
  review → follow-up → re-review chain (docs task t_00b7477c → review
  t_20934411 REQUEST_CHANGES on the §1.2 `reason`-label literal
  `status_5xx` vs the code's `status_<code>` → follow-up t_05272189
  fix commit `bad1763` on the same branch → re-review t_f6eb5302
  Verdict: approve → admin squash merge), with the full deliverable
  detail in the addendum 9 section above.

### In flight

- PR #54 — docs(iteration-notes): addendum 10 to 2026-08-04 note for
  innerwork-doctor scoping merge (#52), still OPEN on
  `feat/iteration-note-2026-08-04-addendum-10` (head `b1993a7`) at
  drafting time. It covers the innerwork-doctor PM scoping (#52,
  merged 2026-08-04T08:46:48Z, squash `8197809`, branch
  `docs/innerwork-doctor-scoping` deleted) and was drafted against
  main @ `8197809` — before PR #53 merged — so its In flight section
  listed PR #53 as still open and its intro framed it as following
  addendum 8's content on `main`. Both framings are superseded: PR
  #53 has merged and addendum 9 now precedes this addendum on `main`.
  When PR #54 merges, addendum 10's landing position will be resolved
  against the current `main` at merge time (its branch predates the
  #53 squash); the addendum 10 section's coverage of #52 is otherwise
  accurate.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #52 on `main`: addendum 9 (#53, merged) recorded the #51
  observability-shape merge, and addendum 10 (PR #54, open) records
  the #52 innerwork-doctor scoping merge. The addenda numbering on
  `main` currently reads 1–9 then 11 — addendum 10 is in flight; when
  PR #54 merges the sequence becomes contiguous 1–11.
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #53 is docs-only (the iteration note itself), and #52's scoping
  deliberately left the roadmap untouched — the innerwork-doctor
  bullet stays under "Directional next → CLI ergonomics" until the
  implementation merges (scoping §1 item 8 defers the shipped-move to
  the implementation PR). The eng implementation task t_88234df1 is
  in flight on `feat/innerwork-doctor` (DO NOT MERGE until the QA
  gate passes).
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 12)

Twelfth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
the note section immediately above: PR #56 — docs(iteration-notes):
addendum 11 to the 2026-08-04 note for PR #53 merge (addendum 9) — at
2026-08-04T10:18:04Z. This is the addendum-11 PR itself landing: it
appends addendum 11 to this file, so addendum 9 (#53, merged
2026-08-04T09:30:33Z) and addendum 11 (#56) are now both on `main`.
Addendum 10 (PR #57, re-opened from the closed PR #54), covering the
innerwork-doctor scoping merge #52, is still in flight. It is
reconciled here so the record is complete.

### Landed (additional)

- #56 — docs(iteration-notes): addendum 11 to 2026-08-04 note for PR
  #53 merge (addendum 9) (merged 2026-08-04T10:18:04Z, squash
  `92471c3`, branch `feat/iteration-note` deleted). The note section
  immediately preceding this one, merged by the orchestrator via admin
  squash (the self-approval rule blocked `gh pr review --approve` for
  the m0n3r0-authored PR). It appends addendum 11 to this file per the
  append-only rule in `docs/post-launch-iteration.md` §3, covering PR
  #53 — the addendum-9 PR itself landing for the observability-shape
  docs merge (#51) — which merged at 2026-08-04T09:30:33Z (squash
  `2b3d425`, branch `feat/iteration-note` deleted). With #56 merged,
  addendum 11's in-flight reference to PR #53 is superseded and both
  addendum 9's and addendum 11's content are on `main`; the full
  deliverable detail is in the addendum 9 and addendum 11 sections
  above.

### In flight

- PR #57 — docs(iteration-notes): addendum 10 to 2026-08-04 note for
  innerwork-doctor scoping merge (#52), still OPEN on
  `feat/iteration-note-2026-08-04-addendum-10` (head `8011ee5`) at
  drafting time. Addendum 11's In flight section recorded this PR as
  #54 (head `b1993a7`); PR #54 was closed at 2026-08-04T10:01:36Z and
  the same branch re-opened as PR #57 with the addendum-10 commit
  rebuilt on the addendum 9 squash (`8011ee5`, parent `2b3d425`) — the
  addendum 10 section content is unchanged and still covers the #52
  innerwork-doctor scoping merge (merged 2026-08-04T08:46:48Z, squash
  `8197809`, branch `docs/innerwork-doctor-scoping` deleted). When PR
  #57 merges, the addenda numbering on `main` becomes contiguous 1–11
  and addendum 10's landing position resolves against the then-current
  `main`.
- PR #55 — feat(cli): innerwork doctor — read-only database validation,
  still OPEN on `feat/innerwork-doctor` (head `69de160`). The eng
  implementation for the #52 scoping; DO NOT MERGE until the QA gate
  passes (task t_88234df1).

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #56 on `main`: addendum 9 (#53, merged) recorded the #51
  observability-shape merge, addendum 11 (#56, merged) records the
  addendum-9 PR itself landing, and addendum 10 (PR #57, open) records
  the #52 innerwork-doctor scoping merge. The addenda numbering on
  `main` reads 1–9 then 11 — addendum 10 is in flight; when PR #57
  merges the sequence becomes contiguous 1–11.
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #56 is docs-only (the iteration note itself). The innerwork-doctor
  bullet stays under "Directional next → CLI ergonomics" until the
  implementation (PR #55) merges.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 10)

Tenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after the window
addendum 9 covers: the innerwork-doctor PM scoping (#52) at
2026-08-04T08:46:48Z. Addendum 9 — covering the observability-shape
merge #51 — landed on `main` via PR #53 (merged 2026-08-04T09:30:33Z,
squash `2b3d425`) while this addendum was being drafted, and addendum
12 (PR #58) landed before this addendum's merge, so on `main` this
addendum follows addendum 12's content. #52 is the
PM-scoping card for roadmap slug `innerwork-doctor`: PM scoping task
t_bf2571f5 completed by the orchestrator, and the eng implementation
task t_88234df1 is now in flight. It is reconciled here so the record
is complete.

### Landed (additional)

- #52 — docs(roadmap): PM scoping for innerwork doctor CLI (merged
  2026-08-04T08:46:48Z, squash `8197809`, branch
  `docs/innerwork-doctor-scoping` deleted). Converts the roadmap
  "Directional next → CLI ergonomics" candidate `slug=innerwork-doctor`
  — "`innerwork doctor` to validate a database file against the current
  schema and surface common operator misconfigurations" — into the new
  `docs/roadmap_innerwork_doctor_scoping.md` (349 lines). Every check
  id is grounded in real schema seams, verified by introspecting a
  fresh store rather than guessed: `domain_store.py` v4
  (`DOMAIN_SCHEMA_VERSION = 4`, 11 domain tables + 8 indexes, additive
  v3→v4 `_ensure_column` migrations), `sql_state_store.py` v1 broker
  DDL (`SQLITE_SCHEMA_VERSION = 1`; the doc calls out that
  `_initialize` UPSERTs `schema_version` on every open — construction
  mutates, so the doctor must never construct `SqliteStateStore`), and
  `audit.py` `_AUDIT_DDL` (`AUDIT_SCHEMA_VERSION = 1` is never
  persisted; `SqliteAuditSink.__init__` runs the DDL on open —
  construction writes). Locks the CLI surface (`innerwork doctor
  [DB_PATH] [--database-url] [--audit-log] [--json]
  [--integrity-check]`), the check catalog, the exit-code contract
  (0 healthy / 1 findings / 2 usage — usage failures are stderr-only
  with stdout empty, even under `--json`, matching the
  `export`/`import` convention), the `--json` report contract (house
  `_print_json` shape), the read-only guarantee (`mode=ro` URI
  connect; no `DomainStore`/`SqliteStateStore`/`SqliteAuditSink`
  construction; `--integrity-check` off by default and no integrity
  claim without the flag), an exact 8-file touch list with a
  must-NOT-touch table (the three DDL authorities stay untouched; the
  expected schema lives in `EXPECTED_*` mirrors in a new
  `src/innerwork/doctor.py` with a drift-guard test), the test plan
  (subprocess `_run_cli` pattern in a new `tests/test_doctor.py`, no
  new fixtures), and the handoff checklist for t_88234df1.
  `docs/roadmap.md` is deliberately unchanged — the bullet stays under
  "Directional next → CLI ergonomics" until the implementation merges
  (scoping §1 item 8 defers the shipped-move to the implementation
  PR). Chain: PM scoping task t_bf2571f5 completed by the orchestrator;
  eng implementation task t_88234df1 now in flight on
  `feat/innerwork-doctor` (branches from `main`, DO NOT MERGE — QA
  gate first).

### In flight

- innerwork-doctor implementation — task t_88234df1 in flight on
  `feat/innerwork-doctor` (branch from `main`), DO NOT MERGE until the
  QA gate passes; expected to follow the house chain (QA → SEC →
  independent review) like time-windowed-metrics #45 and
  backup-restore-upgrade #49 before it.

### Direction notes

- innerwork-doctor is the first roadmap item from the "Directional
  next → CLI ergonomics" section to receive PM scoping. Unlike the
  previous five roadmap changes since the audit sweep (#36, #41, #45,
  #49, #51 all moved a bullet or removed a section header), #52 changes
  `docs/roadmap.md` not at all — the bullet remains under "Directional
  next → CLI ergonomics" by design, with the scoping doc deferring the
  shipped-move to the implementation PR.
- The scoping doc is the first to demand a read-only guarantee from a
  CLI subcommand: `mode=ro` connect plus a hard rule that the doctor
  never constructs `DomainStore`/`SqliteStateStore`/`SqliteAuditSink`
  (all three `__init__`s write to the file), and a drift-guard test
  keeps the `EXPECTED_*` schema mirror honest against the three DDL
  authorities.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 13)

Thirteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #58 — docs(iteration-notes): addendum 12 to the 2026-08-04 note for
PR #56 merge (addendum 11) — at 2026-08-04T16:05:59Z (the addendum-12
section above). This is the addendum-12 PR itself landing: it
appends addendum 12 to this file, so addendum 9 (#53, merged
2026-08-04T09:30:33Z), addendum 11 (#56), and addendum 12 (#58) are now
all on `main`. Addendum 10 (PR #57, re-opened from the closed PR #54),
covering the innerwork-doctor scoping merge #52, merged at
2026-08-04T16:57:10Z (squash `00d1aa8`) while this addendum was in
flight, so on `main` its section sits between addendum 12's and this
one. It is reconciled here so the record is complete.

### Landed (additional)

- #58 — docs(iteration-notes): addendum 12 to 2026-08-04 note for PR
  #56 merge (addendum 11) (merged 2026-08-04T16:05:59Z, squash
  `157f1bd`, branch `feat/iteration-note` deleted). The addendum-12
  section above, merged by the orchestrator via admin
  squash (the self-approval rule blocked `gh pr review --approve` for
  the m0n3r0-authored PR). It appends addendum 12 to this file per the
  append-only rule in `docs/post-launch-iteration.md` §3, covering PR
  #56 — the addendum-11 PR itself landing for PR #53 (addendum 9) —
  which merged at 2026-08-04T10:18:04Z (squash `92471c3`, branch
  `feat/iteration-note` deleted). Addendum 12's review (t_691f911d)
  approved with CI green (GitHub test PASS, actions/runs/30900583726;
  local parity 457 passed, ruff clean, pyright 0 errors). With #58
  merged, addendum 12's in-flight reference to PR #57 is superseded and
  its content is on `main`; the full deliverable detail is in the
  addendum 12 section above.

### In flight

- None — the two PRs tracked as in flight by addendum 12 both merged
  during this addendum's window. PR #57 (addendum 10) merged at
  2026-08-04T16:57:10Z (squash `00d1aa8`, head `043ed78` on
  `feat/iteration-note-2026-08-04-addendum-10`); its addendum-10
  section content is unchanged and still covers the #52 innerwork-doctor
  scoping merge (merged 2026-08-04T08:46:48Z, squash `8197809`, branch
  `docs/innerwork-doctor-scoping` deleted). PR #55 (innerwork-doctor
  implementation) merged at 2026-08-04T17:21:37Z (squash `149ae46`);
  its branch head was `5a0ba0c` — not the `69de160` addendum 12
  recorded (the extra commit is the percent-encode `_connect_ro` URI
  fix, committed 2026-08-04T16:39:06Z) — and the merge moved the
  roadmap bullet to shipped.

### Direction notes

- The iteration-note chain now covers every post-note merge through #58
  on `main`: addendum 9 (#53, merged) recorded the #51
  observability-shape merge, addendum 11 (#56, merged) records the
  addendum-9 PR itself landing, addendum 12 (#58, merged) records the
  addendum-11 PR itself landing, and addendum 10 (PR #57, merged at
  2026-08-04T16:57:10Z) records the #52 innerwork-doctor scoping merge.
  With PR #57 merged and this addendum landing, the addenda numbering
  on `main` is contiguous 1–13.
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #58 is docs-only (the iteration note itself). The innerwork-doctor
  bullet moved to the shipped list when the implementation (PR #55)
  merged at 2026-08-04T17:21:37Z (squash `149ae46`).
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 14)

Fourteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
the note section immediately above: PR #57 — docs(iteration-notes):
addendum 10 to the 2026-08-04 note for innerwork-doctor scoping merge
(#52) — at 2026-08-04T16:57:10Z. This is the addendum-10 PR itself
landing: it appends addendum 10 to this file, so addendum 10's content
(covering the #52 innerwork-doctor scoping merge, merged
2026-08-04T08:46:48Z, squash `8197809`, branch
`docs/innerwork-doctor-scoping` deleted) is now on `main`. Addendum 13
(PR #59, in flight at drafting time) recorded PR #57 as in flight
because #57 merged ~52 minutes after addendum 13 was drafted; with
#57 landed, the addenda on `main` carry the complete label set 1–14
once addendum 13 and this addendum both land — as labels; their
physical section order depends on landing order (if this addendum
lands first, the file order is 9, 11, 12, 10, 14, 13). It is
reconciled here so the record is complete.

### Landed (additional)

- #57 — docs(iteration-notes): addendum 10 to 2026-08-04 note for
  innerwork-doctor scoping merge (#52) (merged 2026-08-04T16:57:10Z,
  squash `00d1aa8`, branch `feat/iteration-note-2026-08-04-addendum-10`
  deleted). The note section immediately preceding this one, merged by
  the orchestrator via `--squash --delete-branch --admin` (the
  self-PR rule rejected `gh pr review --approve` for the
  m0n3r0-authored PR, as expected). Review t_ca4f12af approved; CI
  green (GitHub check `test` PASS, actions/runs/30929513946). It
  appends addendum 10 to this file per the append-only rule in
  `docs/post-launch-iteration.md` §3, covering PR #52 — docs(roadmap):
  PM scoping for innerwork doctor CLI — which merged at
  2026-08-04T08:46:48Z (squash `8197809`, branch
  `docs/innerwork-doctor-scoping` deleted). The addendum-10 section was
  drafted while addenda 11 and 12 were landing (addenda 12 and 13
  recorded its in-flight heads `8011ee5` and `043ed78`); with #57
  merged, addendum 10's content is on `main` and the in-flight
  references in addenda 12 and 13 are superseded. The full deliverable
  detail is in the addendum 10 section above.
- #55 — feat(cli): innerwork doctor — read-only database validation
  (schema, version drift, operator misconfigurations) (merged
  2026-08-04T17:21:37Z, squash `149ae46`, branch
  `feat/innerwork-doctor` deleted). The eng implementation for the #52
  scoping, merged after its re-review (t_5ba0c302) passed: the
  re-review verified the F1 fix (`_connect_ro` now builds
  `f"{path.as_uri()}?mode=ro"` so `?`/`#` filenames can no longer
  truncate the URI, drop `mode=ro`, or create a spurious empty file),
  the `test_uri_filename_special_chars_percent_encoded` regression
  test (tests/test_doctor.py:886), and CI parity (GitHub check `test`
  PASS, actions/runs/30930169289; local 508 passed, ruff clean,
  pyright 0 errors) before the orchestrator merged via admin squash.
  QA gate t_487a74f3 had landed +19 regression tests (50 total in
  tests/test_doctor.py at commit `69de160`) closing previously
  untested branches (empty file, JSON target-null contract, disk_usage
  OSError silence, missing domain version key, extra-column and
  partial-table cases, index/broker version drift); SEC t_3035185a
  passed on the pre-fix head. `docs/roadmap.md` moved the
  innerwork-doctor bullet from "Directional next → CLI ergonomics" to
  shipped — implemented in `src/innerwork/doctor.py` /
  `src/innerwork/cli.py`, documented in `docs/migration-guide.md` §2.5
  (post-phase-10 addition) — the sixth roadmap content change since
  the audit sweep, after #36 (addendum 3), #41 (addendum 5), #45
  (addendum 7), #49 (addendum 8), and #51 (addendum 9).

### In flight

- PR #59 — docs(iteration-notes): addendum 13 to 2026-08-04 note for
  PR #58 merge (addendum 12), still OPEN on `feat/iteration-note`
  (opened 2026-08-04T16:40:21Z) at drafting time. The addendum-13 PR
  itself; when it lands it records PR #58 (addendum 12, merged
  2026-08-04T16:05:59Z, squash `157f1bd`), completing the addenda
  label set 1–13 on `main` (physical order per landing order).
- PR #55 — feat(cli): innerwork doctor — read-only database validation
  (schema, version drift, operator misconfigurations), MERGED at
  2026-08-04T17:21:37Z (squash `149ae46`, branch
  `feat/innerwork-doctor` deleted) — no longer in flight. The eng
  implementation for the #52 scoping, merged after its re-review
  (t_5ba0c302) passed; see the Landed (additional) entry below.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #57 on `main`: addendum 9 (#53, merged) recorded the #51
  observability-shape merge, addendum 10 (#57, merged) records the #52
  innerwork-doctor scoping merge, addendum 11 (#56, merged) records
  the addendum-9 PR itself landing, and addendum 12 (#58, merged)
  records the addendum-11 PR itself landing. Addendum 13 (PR #59,
  open) records the addendum-12 PR itself landing; once it and this
  addendum land, `main` carries the complete addenda label set 1–14
  (physical section order depends on landing order — if PR #59 lands
  after this addendum, the file order is 9, 11, 12, 10, 14, 13).
- `docs/roadmap.md` on `main` is no longer unchanged by this window:
  #55 (merged 2026-08-04T17:21:37Z) moved the innerwork-doctor bullet
  from "Directional next → CLI ergonomics" to shipped — implemented in
  `src/innerwork/doctor.py` / `src/innerwork/cli.py`, documented in
  `docs/migration-guide.md` §2.5 (post-phase-10 addition), the sixth
  roadmap content change since the audit sweep. The "Directional next
  → CLI ergonomics" section keeps only the remaining candidates
  (friendlier `--help` examples, optional shell completion); the C1
  portability permission drop and C15 mixed-case mention handles
  remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 15)

Fifteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. One PR merged after the window
addendum 14 covers: the cli-ergonomics PM scoping (#61) at
2026-08-04T18:23:14Z. Addendum 13 (PR #59) merged at
2026-08-04T19:17:28Z (squash `ffb7428`) while this addendum was in
flight — it records PR #58 (addendum 12 landing) — and addendum 14
(PR #60) merged at 2026-08-04T20:03:56Z (squash `e5e4da7`, branch
`feat/iteration-note-2026-08-04-addendum-14` deleted) while this
addendum was in flight, recording PR #57 (addendum 10 landing). On
`main` this addendum follows addendum 14's section (addendum 14
landed first, so the physical section order is 9, 11, 12, 10, 13, 14,
15). #61 is the PM-scoping card for roadmap slug `cli-ergonomics`; the
eng implementation task t_75de6f68 is now in flight. It is reconciled
here so the record is complete.

### Landed (additional)

- #61 — docs(roadmap): PM scoping for cli-ergonomics (help examples +
  shell completion) (merged 2026-08-04T18:23:14Z, squash `139aa45`,
  branch `docs/cli-ergonomics-scoping` deleted). Converts the roadmap
  "Directional next → CLI ergonomics" candidate `slug=cli-ergonomics`
  — "Friendlier `--help` examples on the migration commands" and
  "Optional shell completion (bash, zsh, fish) emitted by a hidden CLI
  subcommand" — into the new `docs/roadmap_cli_ergonomics_scoping.md`
  (272 lines, 12 sections). Locks a contained additive slice grounded
  in the real parser: the five migration/import/export commands
  (`export`, `import`, `migrate`, `import-markdown`, `import-csv` —
  all five dispatch through `_domain_dispatch`, `cli.py:519`) get
  `epilog=` examples blocks (one example per line, `examples:` on its
  own line, real flags and real fixture paths only), each
  parse-validated against the real `build_parser()` by the new
  `tests/test_cli_ergonomics.py` (11/11 PARSE-OK at scoping time, all
  fixture paths verified to exist); a hidden `completion` subcommand
  registered with `help=argparse.SUPPRESS` (`innerwork completion
  bash|zsh|fish`) emitting static best-effort scripts whose word lists
  are derived from `build_parser()` at emission time (no drift),
  stdlib only, no network/eval/exec; a `RawDescriptionHelpFormatter`
  fix on the five subparsers plus `doctor` (the doctor epilog
  currently renders as a run-on paragraph — the default
  `HelpFormatter` collapses newlines); and a stale-prose fix
  (`docs/migration-guide.md` §2 intro still claims "Phase 10 adds
  three subcommands"). Explicit anti-hallucination guardrail: the
  roadmap task body's illustrative example `innerwork import-markdown
  ./fixtures/docs --space eng` is fabricated (`--space` does not exist
  on `import-markdown`; `./fixtures/docs` does not exist) — scoping §3
  rejects it and the parse-validity test makes that class of error
  mechanically impossible. `docs/roadmap.md` is deliberately unchanged
  — the bullet stays under "Directional next → CLI ergonomics" until
  the implementation merges (scoping §1 item 8 defers the
  shipped-move to the implementation PR). Grounding: verified against
  `main@149ae46` by running the real CLI help and parsing every locked
  example through `build_parser()`; the Copilot opus review was
  unavailable (HTTP 403, license) so programmatic verification plus
  the scoping §8 grep checklist were used instead. Chain: eng
  implementation task t_75de6f68 now in flight on
  `feat/cli-ergonomics` (branches from `main`, DO NOT MERGE — QA gate
  first).
- #60 — docs(iteration-notes): addendum 14 to 2026-08-04 note for PR
  #57 merge (addendum 10 landing) (merged 2026-08-04T20:03:56Z,
  squash `e5e4da7`, branch
  `feat/iteration-note-2026-08-04-addendum-14` deleted). The
  addendum-14 section directly above records PR #57 (addendum 10
  landing, merged 2026-08-04T16:57:10Z, squash `00d1aa8`) and PR #55
  (innerwork-doctor implementation, merged 2026-08-04T17:21:37Z,
  squash `149ae46`).

### In flight

- PR #63 — feat(cli): friendlier --help examples + hidden shell
  completion (bash/zsh/fish), the cli-ergonomics implementation for
  task t_75de6f68, was OPEN in QA gate on `feat/cli-ergonomics`
  (branch from `main`), DO NOT MERGE until the QA gate passes;
  expected to follow the house chain (QA → SEC → independent review)
  like time-windowed-metrics #45, backup-restore-upgrade #49, and
  innerwork-doctor #55 before it. It has since MERGED at
  2026-08-04T21:20:03Z (squash `6a47669`, branch deleted), moving the
  cli-ergonomics roadmap bullet to the shipped list.
- PR #59 — docs(iteration-notes): addendum 13 to 2026-08-04 note for
  PR #58 merge (addendum 12), still OPEN on `feat/iteration-note` at
  drafting time but MERGED while this addendum was in flight (squash
  `ffb7428`, 2026-08-04T19:17:28Z); its addendum-13 section sits
  above addendum 14's and records PR #58 (merged
  2026-08-04T16:05:59Z, squash `157f1bd`).

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #61 on `main`: addendum 13 (PR #59, merged) records the addendum-12
  PR itself landing (#58), addendum 14 (PR #60, merged
  2026-08-04T20:03:56Z) records the addendum-10 PR itself landing
  (#57), and this addendum records the cli-ergonomics PM scoping
  (#61). The addenda numbering on `main` reads 1–14 (addendum 10's
  section sits between addendum 12's and addendum 13's because PR #57
  merged after PR #58; addendum 14's section follows addendum 13's
  because PR #60 landed after PR #59); with this addendum landing, the
  sequence is contiguous 1–15 (physical order 9, 11, 12, 10, 13, 14,
  15).
- cli-ergonomics is the second roadmap item from the "Directional
  next → CLI ergonomics" section to receive PM scoping, after
  innerwork-doctor (#52). Like #52, #61 changes `docs/roadmap.md` not
  at all — the bullet remains under "Directional next → CLI
  ergonomics" by design, with the scoping doc deferring the
  shipped-move to the implementation PR.
- The scoping doc is the first to gate help text mechanically against
  hallucination: every locked example must parse through the real
  `build_parser()`, and §3 names the fabricated roadmap-task-body
  example (`--space` on `import-markdown`) as must-never-appear — the
  implementation worker is explicitly forbidden from inventing a
  `--space` flag to make it work.

## Late merges (addendum 17)

Seventeenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #60 — docs(iteration-notes): addendum 14 to the 2026-08-04 note for
PR #57 merge (addendum 10 landing) — at 2026-08-04T20:03:56Z. This is
the addendum-14 PR itself landing: it appends addendum 14 to this file,
so addendum 14's content — covering PR #57 (the addendum-10 PR itself
landing for the #52 innerwork-doctor scoping merge) and PR #55 (the
innerwork-doctor implementation) — is now fully on `main`. Addendum 14
recorded PR #59 (addendum 13) as in flight at drafting time; PR #59
merged at 2026-08-04T19:17:28Z (squash `ffb7428`, branch
`feat/iteration-note` deleted) before this addendum's subject PR, so on
`main` the addenda label set is now complete 1–14 (physical order 9,
11, 12, 10, 13, 14). It is reconciled here so the record is complete.

### Landed (additional)

- #60 — docs(iteration-notes): addendum 14 to 2026-08-04 note for PR
  #57 merge (addendum 10 landing) (merged 2026-08-04T20:03:56Z, squash
  `e5e4da7`, branch `feat/iteration-note-2026-08-04-addendum-14`
  deleted). The note section immediately preceding this one. It appends
  addendum 14 to this file per the append-only rule in
  `docs/post-launch-iteration.md` §3, covering PR #57 — the addendum-10
  PR itself landing for the #52 innerwork-doctor scoping merge — which
  merged at 2026-08-04T16:57:10Z (squash `00d1aa8`, branch
  `feat/iteration-note-2026-08-04-addendum-10` deleted), plus PR #55
  (innerwork-doctor implementation, merged 2026-08-04T17:21:37Z, squash
  `149ae46`, branch `feat/innerwork-doctor` deleted). With #60 merged,
  addendum 14's in-flight reference to PR #59 is superseded — PR #59
  (addendum 13) merged at 2026-08-04T19:17:28Z (squash `ffb7428`,
  branch `feat/iteration-note` deleted) — and the addenda label set on
  `main` is complete 1–14 (physical order 9, 11, 12, 10, 13, 14). The
  full deliverable detail is in the addendum 14 section above.

### In flight

- PR #62 — docs(iteration-notes): addendum 15 to 2026-08-04 note for
  post-note merge (PR #61 cli-ergonomics scoping), was OPEN on
  `feat/iteration-note-2026-08-04-addendum-15` (head `0c227dd`, later
  `4665b01` after the conflict fix, opened 2026-08-04T18:45:19Z) at
  drafting time; GitHub reported CONFLICTING — conflict fix landed on
  the branch by merging `origin/main` (that merge brought addendum 17
  in). It has since MERGED at 2026-08-04T21:47:19Z (squash
  `eed0438`). It covers PR #61 —
  docs(roadmap): PM scoping for cli-ergonomics (help examples + shell
  completion), merged 2026-08-04T18:23:14Z (squash `139aa45`, branch
  `docs/cli-ergonomics-scoping` deleted).
- PR #64 — docs(iteration-notes): addendum 16 to 2026-08-04 note for
  post-note merge (PR #59 addendum 13 + seam fix landing), still OPEN
  on `feat/iteration-note-2026-08-04-addendum-16` (head `a206561d`,
  opened 2026-08-04T19:31:54Z) at drafting time; GitHub reported
  CONFLICTING — conflict fix landed on the branch by merging
  `origin/main` (this merge brings addendum 15 in), pending re-review.
- PR #63 — feat(cli): friendlier --help examples + hidden shell
  completion (bash/zsh/fish), was OPEN on `feat/cli-ergonomics` (head
  `7c421ad`, opened 2026-08-04T19:05:37Z) at drafting time; GitHub
  reported MERGEABLE, in sec gate. It has since MERGED at
  2026-08-04T21:20:03Z (squash `6a47669`, branch deleted), moving the
  cli-ergonomics roadmap bullet to the shipped list.

### Direction notes

- The iteration-note chain now covers every post-note merge through #60
  on `main`: the addenda label set is complete 1–14, and this addendum
  records the addendum-14 PR itself landing. Addendum 15 (PR #62) had
  its conflict fix landed by merging `origin/main` (addendum 17) into
  its branch and has since MERGED (2026-08-04T21:47:19Z, squash
  `eed0438`); addendum 16 (PR #64) remains in flight with its conflict
  fix landed by merging `origin/main` (addendum 15); PR #63
  (cli-ergonomics implementation) MERGED at 2026-08-04T21:20:03Z
  (squash `6a47669`).
- `docs/roadmap.md` on `main` is no longer unchanged by this
  addendum's window: #60 is docs-only (the iteration note itself), and
  #61's scoping deliberately left the roadmap untouched — but the
  cli-ergonomics implementation (PR #63) merged at 2026-08-04T21:20:03Z
  and moved the bullet to the shipped list.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.
## Late merges (addendum 16)

Sixteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #59 — docs(iteration-notes): addendum 13 to the 2026-08-04 note for
PR #58 merge (addendum 12) — at 2026-08-04T19:17:28Z. This is the
addendum-13 PR itself landing: it appends addendum 13 to this file, so
the addenda label set on `main` is now complete through 13 (physical
section order 1–9, 11, 12, 10, 13 — addendum 10's section sits between
12's and 13's because PR #57 merged after PR #58). The same merge also
landed the addendum-10/addendum-13 seam repair: commit `c6b99a1`
restores addendum 10's C1 closing bullet at the seam, so both sections
end with the complete standard closing pair (C1 + NoBeta). Addendum 14
(PR #60) landed while this addendum was in flight — merged
2026-08-04T20:03:56Z, squash `e5e4da7` — and addendum 17 (PR #65)
landed after it (merged 2026-08-04T20:54:19Z, squash `7a45b63`,
branch `feat/iteration-note-2026-08-04-addendum-17` deleted), so
addendum 17's section now sits between addendum 14's and this one.
Addendum 15 (PR #62) was still in flight at drafting time; it has
since MERGED at 2026-08-04T21:47:19Z (squash `eed0438`, branch
deleted) and its addendum-15 section now sits between addendum 14's
and addendum 17's. It is reconciled here so the record is complete.

### Landed (additional)

- #59 — docs(iteration-notes): addendum 13 to 2026-08-04 note for PR
  #58 merge (addendum 12) (merged 2026-08-04T19:17:28Z, squash
  `ffb7428`, branch `feat/iteration-note` deleted). The note section
  immediately preceding this one, merged by the orchestrator via admin
  squash (the self-approval rule blocked `gh pr review --approve` for
  the m0n3r0-authored PR). It appends addendum 13 to this file per the
  append-only rule in `docs/post-launch-iteration.md` §3, covering PR
  #58 — the addendum-12 PR itself landing for PR #56 (addendum 11) —
  which merged at 2026-08-04T16:05:59Z (squash `157f1bd`, branch
  `feat/iteration-note` deleted). Addendum 13's review approved with CI
  green; the full deliverable detail is in the addendum 13 section
  above. The merge also landed the addendum-10/addendum-13 seam
  repair: addendum 13's insert had displaced addendum 10's C1+NoBeta
  closing pair and orphaned a NoBeta paragraph; commit `c6b99a1`
  (docs(iteration-notes): restore addendum 10 C1 closing bullet at
  addendum-13 seam) restored the C1 bullet so each section ends with
  the complete standard closing pair (addendum 10: C1+NoBeta, addendum
  13: C1+NoBeta), matching the pattern of every prior addendum.

### In flight

- PR #60 — docs(iteration-notes): addendum 14 to 2026-08-04 note for
  PR #57 merge (addendum 10 landing), was OPEN on
  `feat/iteration-note-2026-08-04-addendum-14` (head `bc34d91`) at
  drafting time, with a merge-conflict fix in flight (GitHub
  mergeable CONFLICTING / mergeStateStatus DIRTY against current
  `main`); it has since MERGED at 2026-08-04T20:03:56Z (squash
  `e5e4da7`), and its addendum-14 section sits immediately above
  addendum 17's section (PR #65, landed 2026-08-04T20:54:19Z, squash
  `7a45b63`), which in turn sits immediately above this one. It
  records PR #57 (merged 2026-08-04T16:57:10Z, squash
  `00d1aa8`) — the addendum-10 PR itself landing — and its Landed
  section also carries #55 (innerwork-doctor implementation, merged
  2026-08-04T17:21:37Z, squash `149ae46`).
- PR #62 — docs(iteration-notes): addendum 15 to 2026-08-04 note for
  post-note merge (PR #61 cli-ergonomics scoping), was OPEN on
  `feat/iteration-note-2026-08-04-addendum-15` (head `0c227dd`, later
  `4665b01` after the conflict fix) at drafting time, with a
  merge-conflict fix in flight (GitHub mergeable CONFLICTING /
  mergeStateStatus DIRTY against current `main`); it has since MERGED
  at 2026-08-04T21:47:19Z (squash `eed0438`, branch deleted), and its
  addendum-15 section now sits between addendum 14's and addendum
  17's on `main`. Records PR #61 (cli-ergonomics PM scoping, merged
  2026-08-04T18:23:14Z, squash `139aa45`).
- PR #63 — feat(cli): friendlier --help examples + hidden shell
  completion (bash/zsh/fish), was OPEN on `feat/cli-ergonomics` (head
  `7c421ad`), the cli-ergonomics implementation for the #61 scoping;
  GitHub reported MERGEABLE (mergeStateStatus CLEAN) at resolution
  time, still held in the QA gate (DO NOT MERGE until the QA gate
  passes). It has since MERGED at 2026-08-04T21:20:03Z (squash
  `6a47669`, branch deleted), moving the cli-ergonomics roadmap bullet
  to the shipped list. CI parity on
  the branch: 547 passed, ruff clean, pyright 0 errors.
- PR #66 — docs(architecture): docs/architecture.md — subsystem
  overview for new contributors, OPEN on `feat/architecture-doc`
  (head `4312039`, opened 2026-08-04T21:29:10Z, mergeStateStatus
  CLEAN) at resolution time.
- PR #67 — docs(iteration-notes): addendum 18 to 2026-08-04 note for
  post-note merges (PR #63 cli-ergonomics impl + PR #62 addendum 15
  landing), OPEN on `feat/iteration-note-2026-08-04-addendum-18`
  (head `a5600b5`, later `1bbb82a` after the post-review 546→547 CI
  count fix, opened 2026-08-04T21:57:45Z, mergeStateStatus
  CLEAN) at resolution time.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #61's scoping on `main`: addendum 13 (PR #59, merged) records the
  addendum-12 PR itself landing (#58), addendum 14 (PR #60, merged)
  records the addendum-10 PR itself landing (#57), addendum 17
  (PR #65, merged 2026-08-04T20:54:19Z, squash `7a45b63`) records the
  addendum-14 PR itself landing (#60), and addendum 15 (PR #62,
  merged 2026-08-04T21:47:19Z, squash `eed0438`) records the
  cli-ergonomics PM scoping (#61). The addenda label set
  on `main` is contiguous 1–15 plus 17 (this addendum pending); once
  this addendum lands, the sequence is contiguous 1–17.
- `docs/roadmap.md` on `main` is no longer unchanged by this
  addendum's window: #59 is docs-only (the iteration note itself); the
  innerwork-doctor bullet moved to shipped with #55 (recorded by
  addendum 14, landed); and the cli-ergonomics bullet moved to the
  shipped list with the implementation PR #63 (merged 2026-08-04T21:20:03Z,
  squash `6a47669`).
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 18)

Eighteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. Two PRs merged after the window
addendum 17 covers: PR #63 — feat(cli): friendlier --help examples +
hidden shell completion (bash/zsh/fish), the cli-ergonomics
IMPLEMENTATION — at 2026-08-04T21:20:03Z, and PR #62 —
docs(iteration-notes): addendum 15 to the 2026-08-04 note for the PR
#61 cli-ergonomics scoping merge — at 2026-08-04T21:47:19Z. This is
also the addendum-15 PR itself landing: it appends addendum 15 to
this file, so addendum 15's content — covering PR #61 (the
cli-ergonomics PM scoping) — is now fully on `main`, superseding the
in-flight reference addendum 17 recorded. Addendum 17 recorded PRs
#62, #64, and #63 as in flight at drafting time; #62 and #63 merged
before this addendum's window closed, #64 remains open. It is
reconciled here so the record is complete.

### Landed (additional)

- #63 — feat(cli): friendlier --help examples + hidden shell
  completion (bash/zsh/fish) (merged 2026-08-04T21:20:03Z, squash
  `6a47669`, branch `feat/cli-ergonomics` deleted). The
  cli-ergonomics implementation for the scoping doc locked by PR #61
  (addendum 15's subject): the five migration/import/export commands
  (`export`, `import`, `migrate`, `import-markdown`, `import-csv`)
  ship parse-validated `--help` `examples:` blocks (locked §2 content,
  real flags and real paths only), those five subparsers plus
  `doctor` switch to `argparse.RawDescriptionHelpFormatter` so the
  doctor epilog renders line-separated instead of a run-on paragraph,
  and a hidden `innerwork completion bash|zsh|fish` subcommand
  (`help=argparse.SUPPRESS`, with a `build_parser()` metavar rebuild
  for Python 3.10/3.11) emits static best-effort completion scripts
  whose word lists are derived from `build_parser()` at emission time
  (anti-drift). New files `src/innerwork/completion.py` and
  `tests/test_cli_ergonomics.py` (392 lines); docs:
  `docs/migration-guide.md` §2.6 + §2 intro fix, CHANGELOG entry.
  Eng task t_75de6f68 (in flight per addendum 15) now landed. CI
  parity at merge: 547 passed, ruff clean, pyright 0 errors. This is
  the shipped-move the scoping deferred: `docs/roadmap.md` moves the
  cli-ergonomics bullet from "Directional next → CLI ergonomics" to
  the "Shipped through Phase 10" list (see Direction notes).
- #62 — docs(iteration-notes): addendum 15 to 2026-08-04 note for
  post-note merge (PR #61 cli-ergonomics scoping) (merged
  2026-08-04T21:47:19Z, squash `eed0438` of branch head `4665b01`,
  branch `feat/iteration-note-2026-08-04-addendum-15` deleted). The
  addendum-15 PR itself landing; its section sits directly above
  addendum 17's in this file (physical order 9, 11, 12, 10, 13, 14,
  15, 17). It records PR #61 (merged 2026-08-04T18:23:14Z, squash
  `139aa45`, branch `docs/cli-ergonomics-scoping` deleted) — the PM
  scoping for roadmap slug `cli-ergonomics` (272 lines, 12 sections,
  `docs/roadmap_cli_ergonomics_scoping.md`). GitHub reported
  CONFLICTING while addendum 17 was in flight; the conflict fix
  landed on the branch by merging `origin/main` (`4665b01`), then the
  PR merged cleanly.

### In flight

- PR #64 — docs(iteration-notes): addendum 16 to 2026-08-04 note for
  post-note merge (PR #59 addendum 13 + seam fix landing), still OPEN
  on `feat/iteration-note-2026-08-04-addendum-16` (head `bd1758a`,
  opened 2026-08-04T19:31:54Z) at drafting time; GitHub reports
  CONFLICTING — conflict fix still in flight on the branch.
- PR #66 — docs(architecture): docs/architecture.md — subsystem
  overview for new contributors, OPEN on `feat/architecture-doc`
  (head `4312039`, opened 2026-08-04T21:29:10Z) at drafting time;
  GitHub reports MERGEABLE. Closes the roadmap architecture-doc item;
  not part of the iteration-note chain.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #62/#63 on `main`: the addenda label set is complete 1–15 plus 17
  (physical order 9, 11, 12, 10, 13, 14, 15, 17), and this addendum
  records the cli-ergonomics implementation (#63) and the addendum-15
  PR itself landing (#62). Addendum 16 (PR #64) remains in flight
  with its conflict fix; PR #66 (architecture doc) is a separate
  docs-only line of work, not part of the iteration-note chain.
- `docs/roadmap.md` on `main` now ships the cli-ergonomics bullet:
  PR #63 moved it from "Directional next → CLI ergonomics" to the
  "Shipped through Phase 10" list — exactly the shipped-move that
  scoping §1 item 8 deferred to the implementation PR (addenda 15 and
  17 both noted the bullet stayed put until then). With #55
  (innerwork-doctor) and #63 (cli-ergonomics) landed, two of the
  post-audit roadmap items are now implemented and shipped.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 19)

Nineteenth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #67 — docs(iteration-notes): addendum 18 to the 2026-08-04 note for
post-note merges (PR #63 cli-ergonomics impl + PR #62 addendum 15
landing) — at 2026-08-04T23:57:58Z. This is the addendum-18 PR itself
landing: it appends addendum 18 to this file, so addendum 18's content
— covering PR #63 (the cli-ergonomics implementation) and PR #62
(addendum 15) — is now fully on `main`. Addendum 18 recorded PRs #64
and #66 as in flight at drafting time and listed neither PR #65 (the
addendum-17 PR itself, landed 2026-08-04T20:54:19Z) nor PR #68
(operator-cookbook docs, still open when addendum 18 was drafted) as
landed; #66 merged at 2026-08-04T23:32:09Z before addendum 18 itself
landed, and #68 merged at 2026-08-05T00:13:14Z before this addendum
was drafted. Those landings are reconciled here so the record is
complete.

### Landed (additional)

- #67 — docs(iteration-notes): addendum 18 to 2026-08-04 note for
  post-note merges (PR #63 cli-ergonomics impl + PR #62 addendum 15
  landing) (merged 2026-08-04T23:57:58Z, squash `f2e9d0d`, branch
  `feat/iteration-note-2026-08-04-addendum-18` deleted). The
  addendum-18 PR itself landing; its section sits directly above this
  one in the file (physical order 9, 11, 12, 10, 13, 14, 15, 17, 18).
  It records PR #63 (cli-ergonomics implementation, merged
  2026-08-04T21:20:03Z, squash `6a47669`, branch `feat/cli-ergonomics`
  deleted) and PR #62 (addendum 15, merged 2026-08-04T21:47:19Z,
  squash `eed0438`, branch `feat/iteration-note-2026-08-04-addendum-15`
  deleted). CI parity at merge: 547 passed per the authoritative run —
  the PR's head carried the 546→547 correction commit (`1bbb82a`) for
  addendum 18's PR #63 entry.
- #65 — docs(iteration-notes): addendum 17 to 2026-08-04 note for PR
  #60 merge (addendum 14 landing) (merged 2026-08-04T20:54:19Z, squash
  `7a45b63`, branch `feat/iteration-note-2026-08-04-addendum-17`
  deleted). The addendum-17 PR itself landing; addendum 18 did not
  record it (its "Landed (additional)" covered #63 and #62 only), so
  it is reconciled here. It appends addendum 17, which records PR #60
  (addendum 14 landing, merged 2026-08-04T20:03:56Z, squash
  `e5e4da7`).
- #66 — docs(architecture): docs/architecture.md — subsystem overview
  for new contributors (merged 2026-08-04T23:32:09Z, squash
  `b180e0f`, branch `feat/architecture-doc` deleted). Closes the
  roadmap architecture-doc item; adds `docs/architecture.md` (329
  lines) plus a `docs/site-outline.md` link fix. Addendum 18 listed it
  as in flight at drafting time (head `4312039`, opened
  2026-08-04T21:29:10Z); it merged before addendum 18 itself landed
  and is reconciled here. Not part of the iteration-note chain.
- #68 — docs(operator-cookbook): add docs/operator-cookbook.md common
  operator recipes (merged 2026-08-05T00:13:14Z, squash `8cae5dd`,
  branch `feat/operator-cookbook` deleted). Adds
  `docs/operator-cookbook.md` (346 lines) of common operator recipes
  alongside the operations runbook. The last roadmap item from the
  consolidated backlog; it was still OPEN when the task opening this
  addendum was created (2026-08-05T00:11:47Z) and merged 87 seconds
  later, before this addendum was drafted. With it landed, the
  post-launch backlog is complete (see Direction notes).

### In flight

- PR #64 — docs(iteration-notes): addendum 16 to 2026-08-04 note for
  post-note merge (PR #59 addendum 13 + seam fix landing), still OPEN
  on `feat/iteration-note-2026-08-04-addendum-16` (head `075d00ce`,
  updated 2026-08-04T23:29:42Z, opened 2026-08-04T19:31:54Z) at
  drafting time; GitHub now reports MERGEABLE / CLEAN (the conflict
  fix landed on the branch, superseding addendum 18's CONFLICTING
  report), test check SUCCESS. Under re-review.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #68 on `main`: the addenda label set is complete 1–15, 17, 18, 19
  (physical order 9, 11, 12, 10, 13, 14, 15, 17, 18, 19), and this
  addendum records the addendum-18 PR itself landing (#67) plus the
  landings addendum 18 left unrecorded or in flight: #65 (the
  addendum-17 PR itself), #66 (architecture doc), and #68
  (operator-cookbook). Addendum 16 (PR #64) remains in flight, now
  MERGEABLE.
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #65/#67 are docs-only (the iteration notes themselves) and #66/#68
  added their docs without editing the roadmap — both Documentation
  bullets ("architectural overview", "common operator recipes"
  cookbook) still sit under "Directional next → Documentation" even
  though both documents now ship, and the cli-ergonomics bullet stays
  in the "Shipped through Phase 10" list per addendum 18. A
  roadmap-bullet cleanup for the shipped docs would go through the
  "How the roadmap changes" PR process if desired.
- With PR #68 (operator-cookbook docs) merged at
  2026-08-05T00:13:14Z, the last roadmap item from the consolidated
  backlog has landed: the post-launch backlog is complete.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.

## Late merges (addendum 20)

Twentieth append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #64 — docs(iteration-notes): addendum 16 to the 2026-08-04 note for
post-note merge (PR #59 addendum 13 + seam fix landing) — at
2026-08-05T00:37:56Z. This is the addendum-16 PR itself landing: it
appends addendum 16 to this file, so addendum 16's content — covering
PR #59 (the addendum-13 PR itself landing) plus the
addendum-10/addendum-13 seam repair (commit `c6b99a1` restoring
addendum 10's C1 closing bullet) — is now fully on `main`. On `main`
addendum 16's section sits between addendum 17's and addendum 18's
(physical order 9, 11, 12, 10, 13, 14, 15, 17, 16, 18). Addendum 16
listed PRs #66 and #67 as in flight at drafting time; both have since
merged — #66 (architecture doc) at 2026-08-04T23:32:09Z (squash
`b180e0f`) and #67 (addendum 18) at 2026-08-04T23:57:58Z (squash
`f2e9d0d`). Addendum 19 (PR #69) records those landings in detail; it merged
2026-08-05T01:21:14Z (squash `9150176`) while this addendum was in
flight, and its section now sits directly above this one. It is
reconciled here so the record is complete.

### Landed (additional)

- #64 — docs(iteration-notes): addendum 16 to 2026-08-04 note for
  post-note merge (PR #59 addendum 13 + seam fix landing) (merged
  2026-08-05T00:37:56Z, squash `729fb9c`, branch
  `feat/iteration-note-2026-08-04-addendum-16` deleted). The
  addendum-16 PR itself landing, merged by the orchestrator via
  `--squash --admin` after re-review (the self-PR rule rejected
  `gh pr review --approve` for the m0n3r0-authored PR); final PR head
  `075d00ce` after the conflict-fix pass (addendum 18's section
  recorded it as CONFLICTING at head `bd1758a`). Its addendum-16
  section now sits between addendum 17's and addendum 18's in this
  file. It records PR #59 — docs(iteration-notes): addendum 13 to the
  2026-08-04 note for PR #58 merge (addendum 12) — which merged
  2026-08-04T19:17:28Z (squash `ffb7428`, branch `feat/iteration-note`
  deleted), plus the addendum-10/addendum-13 seam repair (commit
  `c6b99a1` restoring addendum 10's C1 closing bullet at the seam).
  CI parity per the PR body: 508 passed, ruff clean, pyright 0 errors.
  Full deliverable detail is in the addendum 16 section above.

- #69 — docs(iteration-notes): addendum 19 to 2026-08-04 note for
  post-note merges (PR #67 addendum 18 landing + PR #65/#66/#68
  reconciliations) (merged 2026-08-05T01:21:14Z, squash `9150176`,
  branch `feat/iteration-note-2026-08-04-addendum-19` deleted; final
  PR head `442b141`). The addendum-19 PR itself landing, merged while
  this addendum was in flight; its section sits directly above this
  one in the file (physical order 9, 11, 12, 10, 13, 14, 15, 17, 16,
  18, 19, 20). It records the landings addendum 18 did not record —
  PR #67 (addendum 18 itself, 2026-08-04T23:57:58Z), PR #65
  (addendum 17 itself, 2026-08-04T20:54:19Z), PR #66 (architecture
  doc, 23:32:09Z), and PR #68 (operator-cookbook docs,
  2026-08-05T00:13:14Z) — and listed PR #64 as in flight at drafting
  time; that reference is superseded by this addendum. Addendum 18's
  own in-flight list (PR #64, PR #66) is likewise superseded: #66
  merged at 2026-08-04T23:32:09Z and #64 here.

## Late merges (addendum 21)

Twenty-first append-only addendum, per the append-only rule in
`docs/post-launch-iteration.md` §3. The merge this addendum records is
PR #69 — docs(iteration-notes): addendum 19 to the 2026-08-04 note for
post-note merges (PR #67 addendum 18 landing + PR #65/#66/#68
reconciliations) — at 2026-08-05T01:21:14Z. This is the addendum-19 PR
itself landing: it appends addendum 19 to this file, so addendum 19's
content — covering PR #67 (the addendum-18 PR itself landing), PR #65
(the addendum-17 PR itself), PR #66 (the architecture doc), and PR
#68 (operator-cookbook) — is now fully on `main`, superseding the
in-flight references addendum 17 and 18 recorded. Addendum 19 recorded
PR #64 as in flight at drafting time (under re-review, MERGEABLE);
PR #64 merged at 2026-08-05T00:37:56Z before this addendum's subject
PR landed, and the addendum covering that landing — PR #70, addendum
20 — is itself OPEN under review at drafting time. The PR #68 entry
called out in review t_abecc047 is already recorded in addendum 19's
Landed section (squash `8cae5dd`, merged 2026-08-05T00:13:14Z), so it
is not re-recorded here. It is reconciled here so the record is
complete.

### Landed (additional)

- #69 — docs(iteration-notes): addendum 19 to 2026-08-04 note for
  post-note merges (PR #67 addendum 18 landing + PR #65/#66/#68
  reconciliations) (merged 2026-08-05T01:21:14Z, squash `9150176c`,
  branch `feat/iteration-note-2026-08-04-addendum-19` deleted). The
  addendum-19 PR itself landing; its section sits directly above this
  one in the file (physical order 9, 11, 12, 10, 13, 14, 15, 17, 16,
  18, 19). It records PR #67 (the addendum-18 PR itself, merged
  2026-08-04T23:57:58Z, squash `f2e9d0d`), PR #65 (the addendum-17 PR
  itself, merged 2026-08-04T20:54:19Z, squash `7a45b63`), PR #66
  (architecture doc, merged 2026-08-04T23:32:09Z, squash `b180e0f`),
  and PR #68 (operator-cookbook, merged 2026-08-05T00:13:14Z, squash
  `8cae5dd`). CI at merge: test=pass.

### In flight

- PR #70 — docs(iteration-notes): addendum 20 to 2026-08-04 note for
  post-note merge (PR #64 addendum 16 landing), OPEN on
  `feat/iteration-note-2026-08-04-addendum-20` (opened
  2026-08-05T00:52:39Z) at drafting time, under review (gate
  t_7c907497). It covers PR #64 — docs(iteration-notes): addendum 16
  to 2026-08-04 note for post-note merge (PR #59 addendum 13 + seam
  fix landing), merged 2026-08-05T00:37:56Z (squash `729fb9c`, branch
  `feat/iteration-note-2026-08-04-addendum-16` deleted) — the
  addendum-16 PR itself landing.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #69 on `main`: addendum 16 (PR #64, merged 2026-08-05T00:37:56Z)
  records the addendum-13 PR itself landing (#59) and the seam repair,
  and addendum 18 (PR #67, merged 2026-08-04T23:57:58Z, squash
  `f2e9d0d`) records PR #63 and PR #62. Addendum 19 (PR #69) merged
  2026-08-05T01:21:14Z (squash `9150176`) while this addendum was in
  flight and is recorded in the section above; this addendum records
  PR #64's landing. Addendum 21 (PR #71) merged 2026-08-08T12:34:01Z
  (squash `ae17f18`) while this addendum was still in flight; its
  section sits directly below this one. Once PR #70 lands, the addenda
  label set on `main` is complete 1–21 (physical order 9, 11, 12, 10,
  13, 14, 15, 17, 16, 18, 19, 20, 21).
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #64 is docs-only (the iteration note itself). The last roadmap
  content change remains #63's cli-ergonomics shipped-move, recorded
  in addendum 18.

### Direction notes

- The iteration-note chain now covers every post-note merge through
  #69 on `main`: the addenda label set is complete 1–15, 17, 18, 19
  (physical order 9, 11, 12, 10, 13, 14, 15, 17, 16, 18, 19), and
  this addendum records the addendum-19 PR itself landing (#69).
  Addendum 16 (PR #64) landed at 2026-08-05T00:37:56Z before this
  addendum's subject PR; the addendum covering that landing —
  addendum 20, PR #70 — is OPEN under review (gate t_7c907497) at
  drafting time. Addendum 21 (this PR, #71) merged 2026-08-08T12:34:01Z
  (squash `ae17f18`) while PR #70 remained open; once PR #70 lands,
  the addenda label set on `main` is complete 1–21 and the
  iteration-note chain has covered every post-note merge.
- PR #68 (operator-cookbook docs, merged 2026-08-05T00:13:14Z) was
  the last roadmap backlog item; addendum 19 already recorded its
  entry (confirmed against review t_abecc047's expectation), and this
  addendum adds no further record. After PR #70 (addendum 20) lands,
  the post-launch roadmap backlog is complete.
- `docs/roadmap.md` on `main` is unchanged by this addendum's window:
  #69 is docs-only (the iteration note itself), and addendum 19
  already noted the two Documentation bullets ("architectural
  overview", "common operator recipes" cookbook) still sit under
  "Directional next → Documentation" pending a roadmap-bullet cleanup
  through the "How the roadmap changes" PR process.
- The C1 portability permission drop and C15 mixed-case mention
  handles remain open fix items from the consolidated backlog.
- No beta issues filed; the graduation review described in
  `docs/post-launch-iteration.md` §6 remains open for a future tick.
  No beta-user claims, no participant counts, and no further
  roadmap-priority changes are introduced by this addendum.
